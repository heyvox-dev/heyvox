---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - vox/__init__.py
  - vox/__main__.py
  - vox/main.py
  - vox/audio/__init__.py
  - vox/audio/mic.py
  - vox/audio/wakeword.py
  - vox/audio/stt.py
  - vox/audio/tts.py
  - vox/audio/cues.py
  - vox/input/__init__.py
  - vox/input/ptt.py
  - vox/input/injection.py
  - vox/hud/__init__.py
  - vox/hud/overlay.py
  - vox/hud/ipc.py
  - vox/mcp/__init__.py
  - vox/mcp/server.py
  - vox/adapters/__init__.py
  - vox/adapters/base.py
  - vox/adapters/generic.py
  - vox/cli.py
  - vox/constants.py
  - cues/listening.aiff
  - cues/ok.aiff
  - cues/paused.aiff
  - cues/sending.aiff
  - com.vox.listener.plist
autonomous: true

must_haves:
  truths:
    - "`pip install -e .` succeeds without errors"
    - "`vox` CLI entry point is registered and responds to --help"
    - "Package has modular structure with vox/audio/, vox/input/, vox/hud/, vox/mcp/, vox/adapters/"
    - "All runtime imports from the monolith have corresponding pyproject.toml dependencies"
    - "Source code is extracted into correct modules (not empty placeholder files)"
  artifacts:
    - path: "pyproject.toml"
      provides: "Package definition with all dependencies"
      contains: "vox.cli:main"
    - path: "vox/__init__.py"
      provides: "Package root with version"
      contains: "__version__"
    - path: "vox/cli.py"
      provides: "CLI entry point with subcommands"
      contains: "argparse"
    - path: "vox/main.py"
      provides: "Main event loop (extracted from wake_word_listener.py)"
      min_lines: 50
    - path: "vox/audio/mic.py"
      provides: "Microphone management"
      contains: "find_best_mic"
    - path: "vox/audio/stt.py"
      provides: "STT engine initialization and transcription"
      contains: "transcribe_audio"
    - path: "vox/input/ptt.py"
      provides: "Push-to-talk event tap"
      contains: "start_ptt_listener"
    - path: "vox/input/injection.py"
      provides: "Text injection via clipboard"
      contains: "type_text"
    - path: "vox/hud/overlay.py"
      provides: "Recording indicator (extracted from recording_indicator.py)"
      min_lines: 20
  key_links:
    - from: "pyproject.toml"
      to: "vox/cli.py"
      via: "project.scripts entry point"
      pattern: 'vox = "vox.cli:main"'
    - from: "vox/cli.py"
      to: "vox/main.py"
      via: "lazy import on 'start' command"
      pattern: "from vox"
---

<objective>
Create the vox Python package skeleton with proper pyproject.toml, modular directory structure, and extract all source code from the existing monolith into the correct modules.

Purpose: Establish the installable package structure that all subsequent plans build on. Without this, nothing can be imported, installed, or tested.
Output: A `pip install -e .` installable package with `vox` CLI entry point and all source code distributed into the target module structure.
</objective>

<execution_context>
@/Users/work/.claude/get-shit-done/workflows/execute-plan.md
@/Users/work/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md

Source files to extract from:
@/Users/work/conductor/workspaces/conductor-and-process/manama/wake-word/wake_word_listener.py
@/Users/work/conductor/workspaces/conductor-and-process/manama/wake-word/recording_indicator.py
@/Users/work/conductor/workspaces/conductor-and-process/manama/wake-word/pyproject.toml
@/Users/work/conductor/workspaces/conductor-and-process/manama/wake-word/config.yaml
@/Users/work/conductor/workspaces/conductor-and-process/manama/wake-word/ww
@/Users/work/conductor/workspaces/conductor-and-process/manama/wake-word/com.wakeword.listener.plist
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create pyproject.toml and package skeleton with all modules</name>
  <files>
    pyproject.toml
    vox/__init__.py
    vox/__main__.py
    vox/cli.py
    vox/constants.py
    vox/audio/__init__.py
    vox/input/__init__.py
    vox/hud/__init__.py
    vox/mcp/__init__.py
    vox/adapters/__init__.py
  </files>
  <action>
    1. Create `pyproject.toml` with hatchling build backend:
       - name: "vox-voice" (since "vox" is taken on PyPI)
       - version: "0.1.0"
       - requires-python: ">=3.12"
       - Core dependencies (ALL of these, traced from wake_word_listener.py imports):
         - openwakeword>=0.6.0
         - pyaudio>=0.2.14
         - numpy>=1.24.0
         - PyYAML>=6.0
         - pydantic>=2.0
         - platformdirs>=4.0
         - sherpa-onnx>=1.0
         - pyobjc-framework-Cocoa>=10.0
         - pyobjc-framework-Quartz>=10.0
       - Optional dependencies:
         - `apple-silicon = ["mlx-whisper>=0.1.0"]`
         - `dev = ["pytest", "ruff"]`
       - Scripts: `vox = "vox.cli:main"`
       - Build system: hatchling

    2. Create `vox/__init__.py` with `__version__ = "0.1.0"` only.

    3. Create `vox/__main__.py` with:
       ```python
       from vox.cli import main
       main()
       ```

    4. Create `vox/cli.py` with argparse-based CLI (from research code example):
       - Subcommands: start, stop, restart, status, setup, logs (with -f/--follow flag)
       - NO top-level heavy imports (lazy import pattern for commands that need audio)
       - `start` command: lazy import `from vox.main import run` and call it
       - Other commands: print placeholder messages for now (Phase 3 implements service management)
       - `status` should print "Vox v{version} - service management not yet implemented"

    5. Create `vox/constants.py` with shared constants:
       - `RECORDING_FLAG = "/tmp/vox-recording"` (was /tmp/claude-ww-recording)
       - `LOG_FILE = "/tmp/vox.log"` (was /tmp/wake-word-listener.log)
       - `LAUNCHD_LABEL = "com.vox.listener"` (was com.wakeword.listener)
       - `DEFAULT_SAMPLE_RATE = 16000`
       - `DEFAULT_CHUNK_SIZE = 1280`

    6. Create empty `__init__.py` files for all subpackages: audio, input, hud, mcp, adapters.
  </action>
  <verify>
    Run `pip install -e .` in the project root. Verify it succeeds.
    Run `vox --help` and confirm it shows the subcommand list.
    Run `python -c "from vox import __version__; print(__version__)"` and confirm "0.1.0".
  </verify>
  <done>
    `pip install -e .` succeeds. `vox --help` prints usage with start/stop/restart/status/setup/logs subcommands. `vox` package is importable with correct version.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extract monolith code into modular files and copy assets</name>
  <files>
    vox/main.py
    vox/audio/mic.py
    vox/audio/wakeword.py
    vox/audio/stt.py
    vox/audio/tts.py
    vox/audio/cues.py
    vox/input/ptt.py
    vox/input/injection.py
    vox/hud/overlay.py
    vox/hud/ipc.py
    vox/mcp/server.py
    vox/adapters/base.py
    vox/adapters/generic.py
    cues/listening.aiff
    cues/ok.aiff
    cues/paused.aiff
    cues/sending.aiff
    com.vox.listener.plist
  </files>
  <action>
    Extract code from `/Users/work/conductor/workspaces/conductor-and-process/manama/wake-word/wake_word_listener.py` into the module structure. In this task, move code AS-IS into the right files. Do NOT refactor logic, do NOT add config system yet (Plan 02 handles that). Do update imports between modules.

    **Critical rule:** Each function keeps its existing logic. The only changes are: (a) functions receive parameters instead of reading globals, (b) imports point to sibling modules, (c) module-level globals that were config become function parameters with the same default values.

    1. `vox/audio/mic.py` — Extract from wake_word_listener.py:
       - `find_best_mic(pa, mic_priority, sample_rate, chunk_size)` (lines 582-633)
       - `open_mic_stream(pa, dev_index, sample_rate, chunk_size)` (lines 636-640)
       - Functions take sample_rate, chunk_size, mic_priority as parameters instead of globals

    2. `vox/audio/wakeword.py` — Thin wrapper:
       - `load_models(start_word, stop_word, models_dir)` — loads openwakeword Model (from lines 803-815)
       - Returns the Model instance and metadata (use_separate_words flag)

    3. `vox/audio/stt.py` — Extract:
       - `init_local_stt(engine, mlx_model, model_dir, language, threads)` (lines 129-164)
       - `transcribe_audio(audio_chunks, engine, mlx_model, language, sample_rate)` (lines 167-193)
       - Keep the lazy `import mlx_whisper` and `import sherpa_onnx` inside the functions (not top-level)

    4. `vox/audio/tts.py` — Placeholder module with docstring:
       - `def execute_voice_command(action_key, feedback, tts_script_path)` — extracted from lines 299-307
       - `VOICE_COMMANDS` dict (lines 274-281) and `check_voice_command(text)` function (lines 291-297)
       - The lambda actions (lines 283-288) become a function that takes tts_script_path as parameter
       - If tts_script_path is None, log a warning and return (graceful degradation placeholder)

    5. `vox/audio/cues.py` — Extract:
       - `audio_cue(name, cues_dir)` (lines 199-212)
       - `get_cues_dir()` that resolves the cues directory (looks in package dir, then config)

    6. `vox/input/ptt.py` — Extract:
       - `_PTT_KEY_FLAGS` dict (lines 647-655)
       - `start_ptt_listener(ptt_key, callbacks)` (lines 658-759)
       - callbacks is a dict with `on_start`, `on_stop`, `on_cancel` callables (instead of calling start_recording/stop_recording directly)
       - Keep the Quartz lazy import inside the function

    7. `vox/input/injection.py` — Extract:
       - `type_text(text)` (lines 310-338)
       - `press_enter(count)` (lines 341-350)
       - `focus_app(app_name)` (lines 353-358)
       - `get_clipboard_text()` (lines 377-382)
       - `clipboard_is_image()` (lines 368-374)
       - `SUBPROCESS_TIMEOUT = 5` as module constant

    8. `vox/hud/overlay.py` — Extract the entire recording_indicator.py content:
       - Copy the `main()` function from recording_indicator.py as-is
       - This will be decoupled from Conductor in Plan 02

    9. `vox/hud/ipc.py` — Placeholder with docstring: "Unix socket IPC for HUD communication. Implemented in Phase 5."

    10. `vox/mcp/server.py` — Placeholder with docstring: "MCP voice server. Implemented in Phase 4."

    11. `vox/adapters/base.py` — Create the AgentAdapter protocol:
        ```python
        from typing import Protocol

        class AgentAdapter(Protocol):
            def inject_text(self, text: str) -> None: ...
            def should_auto_send(self) -> bool: ...
        ```

    12. `vox/adapters/generic.py` — Generic adapter that pastes into focused app:
        - Implements AgentAdapter
        - `inject_text` calls `type_text` from vox.input.injection
        - `should_auto_send` returns False (user sends manually)

    13. `vox/main.py` — The main event loop:
        - Extract the `main()` function (lines 766-940)
        - It imports from the sibling modules: `from vox.audio.mic import find_best_mic, open_mic_stream` etc.
        - State variables (is_recording, busy, etc.) stay as module-level in main.py for now
        - Helper functions `start_recording()`, `stop_recording()`, `_send_local()`, `_send_superwhisper()` stay here
        - `log()` function stays here (or in a shared utils, but keep it simple — here is fine)
        - Add a `run()` function that cli.py calls (just calls main())

    14. Copy audio cue files from source:
        ```bash
        cp /Users/work/conductor/workspaces/conductor-and-process/manama/wake-word/cues/*.aiff cues/
        ```

    15. Create `com.vox.listener.plist` — copy from source but update:
        - Label: "com.vox.listener" (was "com.wakeword.listener")
        - ProgramArguments: use `vox` entry point pattern (python -m vox)
        - StandardOutPath: /tmp/vox.stdout.log
        - StandardErrorPath: /tmp/vox.stderr.log
  </action>
  <verify>
    Run `python -c "from vox.audio.mic import find_best_mic; print('OK')"` — confirms import works.
    Run `python -c "from vox.audio.stt import transcribe_audio; print('OK')"` — confirms import works.
    Run `python -c "from vox.input.injection import type_text; print('OK')"` — confirms import works.
    Run `python -c "from vox.input.ptt import start_ptt_listener; print('OK')"` — confirms import works.
    Run `python -c "from vox.hud.overlay import main; print('OK')"` — confirms import works.
    Run `python -c "from vox.adapters.base import AgentAdapter; print('OK')"` — confirms import works.
    Run `python -c "from vox.main import run; print('OK')"` — confirms main module imports work.
    Verify cues/ directory has 4 .aiff files.
    Verify com.vox.listener.plist contains "com.vox.listener" label.
  </verify>
  <done>
    All source code from wake_word_listener.py and recording_indicator.py is distributed into the modular structure. Each module is independently importable. Audio cue files are copied. Plist template is updated with vox branding. No logic has been changed — only code location and import paths.
  </done>
</task>

</tasks>

<verification>
1. `pip install -e .` completes without errors
2. `vox --help` shows subcommands
3. All module imports succeed (vox.audio.*, vox.input.*, vox.hud.*, vox.adapters.*)
4. `ls vox/audio/ vox/input/ vox/hud/ vox/mcp/ vox/adapters/` shows all expected files
5. `grep -r "com.wakeword" .` returns no results in the new package (only in referenced source)
6. cues/ directory contains 4 .aiff files
</verification>

<success_criteria>
- Package installs with `pip install -e .` and `vox` CLI is registered
- Modular structure matches CLAUDE.md target: vox/audio/, vox/input/, vox/hud/, vox/mcp/, vox/adapters/
- All functions from the monolith exist in their target modules with correct signatures
- No code was lost in the extraction (every function from wake_word_listener.py has a home)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
