---
phase: 03-cli-tts-output
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - vox/cli.py
  - vox/main.py
  - vox/setup/__init__.py
  - vox/setup/wizard.py
  - vox/setup/permissions.py
  - vox/setup/launchd.py
autonomous: true

must_haves:
  truths:
    - "vox stop stops the running launchd service"
    - "vox start loads and starts the launchd service via launchctl bootstrap"
    - "vox restart stops then starts the service"
    - "vox status shows running/stopped state with PID"
    - "vox logs tails the service log file"
    - "vox setup walks through permission checks, model download, and mic test"
    - "vox setup opens correct macOS System Settings panes when permissions are missing"
    - "vox setup downloads Kokoro model with progress indication"
    - "vox setup writes Vox MCP server entry to ~/.claude/settings.json allowlist for auto-approval"
    - "TTS interrupt is called when recording starts (wake word or PTT), pausing any active speech"
    - "TTS worker is shut down cleanly in main.py finally block"
  artifacts:
    - path: "vox/setup/wizard.py"
      provides: "Interactive setup wizard with permission checks, model download, mic test, MCP auto-approve"
      exports: ["run_setup"]
    - path: "vox/setup/permissions.py"
      provides: "macOS permission checking and deep-link opening"
      exports: ["check_accessibility", "check_microphone", "open_permission_settings"]
    - path: "vox/setup/launchd.py"
      provides: "launchd plist generation and service management"
      exports: ["bootstrap", "bootout", "status", "write_plist"]
    - path: "vox/cli.py"
      provides: "Complete CLI with all subcommands wired including logs"
      contains: "_cmd_logs"
    - path: "vox/main.py"
      provides: "TTS worker startup, interrupt wiring, and clean shutdown"
      contains: "tts.start_worker"
  key_links:
    - from: "vox/cli.py"
      to: "vox/setup/launchd.py"
      via: "start/stop/restart/status commands calling launchd functions"
      pattern: "from vox\\.setup\\.launchd import"
    - from: "vox/cli.py"
      to: "vox/setup/wizard.py"
      via: "setup command calling run_setup()"
      pattern: "from vox\\.setup\\.wizard import"
    - from: "vox/setup/launchd.py"
      to: "launchctl"
      via: "subprocess calls to bootstrap/bootout/list"
      pattern: "launchctl"
    - from: "vox/main.py"
      to: "vox/audio/tts.py"
      via: "start_worker() at startup, interrupt() in start_recording()"
      pattern: "from vox\\.audio\\.tts import"
    - from: "vox/main.py"
      to: "vox/audio/tts.py"
      via: "shutdown() in finally block for clean TTS worker teardown"
      pattern: "_shutdown_tts"
    - from: "vox/setup/wizard.py"
      to: "~/.claude/settings.json"
      via: "Step 7 writes MCP server allowlist entry"
      pattern: "settings\\.json"
---

<objective>
Implement launchd service management (start/stop/restart/status/logs), the interactive setup wizard (permissions, model download, mic test, MCP auto-approve), and wire TTS interrupt into the main event loop so speech pauses when the user starts speaking.

Purpose: This completes Phase 3 — after this plan, Vox is a fully manageable service with guided onboarding and bidirectional voice (voice-in from Phase 2, voice-out from Plan 01, control surfaces from this plan).

Output: Setup wizard module, launchd management module, complete CLI, and TTS integration in main loop.
</objective>

<execution_context>
@/Users/work/.claude/get-shit-done/workflows/execute-plan.md
@/Users/work/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-cli-tts-output/03-RESEARCH.md
@.planning/phases/03-cli-tts-output/03-01-SUMMARY.md
@vox/cli.py
@vox/main.py
@vox/constants.py
@vox/config.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: launchd service management and setup wizard with MCP auto-approve</name>
  <files>vox/setup/__init__.py, vox/setup/launchd.py, vox/setup/permissions.py, vox/setup/wizard.py</files>
  <action>
Create the `vox/setup/` package with three modules:

**vox/setup/__init__.py** — empty, just makes it a package.

**vox/setup/launchd.py** — Service lifecycle management (CLI-01, PROJ-05):

1. `PLIST_DIR = Path.home() / "Library" / "LaunchAgents"`
2. `PLIST_PATH = PLIST_DIR / f"{LAUNCHD_LABEL}.plist"` (import LAUNCHD_LABEL from constants)
3. `GUI_DOMAIN = f"gui/{os.getuid()}"`

4. `write_plist() -> Path`:
   - Generate plist XML using `sys.executable` for ProgramArguments (NOT hardcoded path — research pitfall 3).
   - ProgramArguments: `[sys.executable, "-m", "vox.main"]` (run main module directly, not CLI start, to avoid re-entrant launchctl).
   - RunAtLoad: true, KeepAlive: true.
   - StandardOutPath and StandardErrorPath: `/tmp/vox.log`.
   - EnvironmentVariables: PATH includes `/usr/local/bin:/usr/bin:/bin` plus the directory of sys.executable.
   - Label: `com.vox.listener` from constants.
   - Write to PLIST_PATH, creating PLIST_DIR if needed.
   - Return PLIST_PATH.

5. `bootstrap() -> tuple[bool, str]`:
   - Check if plist exists, if not call write_plist().
   - Run `launchctl bootstrap gui/{uid} {plist_path}`.
   - Return (success, message). If returncode 37 (already loaded), return (True, "Already running").

6. `bootout() -> tuple[bool, str]`:
   - Run `launchctl bootout gui/{uid} {plist_path}`.
   - Return (success, message). If returncode 3 (not loaded), return (True, "Not running").

7. `get_status() -> dict`:
   - Run `launchctl list com.vox.listener`.
   - Parse output: PID (tab) ExitCode (tab) Label.
   - Return `{"loaded": bool, "running": bool, "pid": int|None, "exit_code": int|None}`.
   - If returncode != 0: return loaded=False, running=False.

8. `restart() -> tuple[bool, str]`:
   - bootout() then bootstrap(). Return combined message.

**vox/setup/permissions.py** — macOS permission checks (CLI-03):

1. `PERMISSION_URLS` dict mapping "microphone", "accessibility", "screen_recording" to their `x-apple.systempreferences:` deep-link URLs (from research Pattern 5).

2. `check_accessibility() -> bool`:
   - Import `ApplicationServices` (PyObjC) lazily.
   - Call `ApplicationServices.AXIsProcessTrusted()`.
   - Return bool.

3. `check_microphone() -> bool`:
   - Try to open a brief pyaudio stream (1 chunk read). If it works, mic is accessible.
   - Wrap in try/except — PermissionError or OSError means no permission.
   - Close stream immediately after test.

4. `check_screen_recording() -> bool`:
   - Run `osascript -e 'tell application "System Events" to get name of first window of first process'`.
   - If it returns successfully, Screen Recording is granted. If error, it's denied.
   - This is a heuristic — Screen Recording is needed for text injection via osascript.

5. `open_permission_settings(permission: str) -> None`:
   - `subprocess.run(["open", PERMISSION_URLS[permission]])`.

**vox/setup/wizard.py** — Interactive guided setup (CLI-02, CLI-03, CLI-04):

Use `rich` for all terminal output (Progress, Console, Panel, Spinner).

`run_setup(config: VoxConfig) -> None`:

Step 1: **Welcome banner** — rich Panel with "Vox Setup" title, version.

Step 2: **Permission checks** (CLI-03):
- Check each: accessibility, microphone, screen_recording.
- For each: show green checkmark if OK, red X if missing.
- If missing: print explanation, call `open_permission_settings()`, prompt user to grant and press Enter to re-check.
- Loop re-check up to 3 times per permission.

Step 3: **Model download** (CLI-04):
- Check if Kokoro model is cached: look for `~/.cache/huggingface/hub/models--hexgrad--Kokoro-82M/`.
- If not cached: show rich Progress with spinner, call `huggingface_hub.snapshot_download(repo_id="hexgrad/Kokoro-82M")`.
- Warn about download size (~300MB) and PyTorch dependency.
- If already cached: show green checkmark "Kokoro model already downloaded".

Step 4: **Mic test**:
- Open a 2-second pyaudio recording.
- Show live amplitude level using rich Live display.
- Report max detected level and whether it seems to be working (level > 100 = OK, level == 0 = problem).

Step 5: **Config file**:
- If `~/.config/vox/config.yaml` doesn't exist, offer to create it via `ensure_config_dir()`.
- Show path to config file.

Step 6: **launchd service**:
- Offer to install the launchd plist via `write_plist()`.
- Offer to start the service via `bootstrap()`.

Step 7: **MCP auto-approve** (CLI-02):
- Check if `~/.claude/settings.json` exists.
- If it exists, read it as JSON. Look for `mcpServers` key.
- Add or update a `vox` entry under `mcpServers` with the MCP server command: `{"command": sys.executable, "args": ["-m", "vox.mcp.server"]}`.
- Also add `vox` to the `allowedTools` list if that key exists (Claude Code auto-approve).
- Write back the updated JSON.
- If `~/.claude/settings.json` does not exist, create it with the vox MCP entry.
- Print confirmation: "Vox MCP server added to Claude Code settings."
- If writing fails (permissions, etc.), print the manual instructions instead.

Step 8: **Summary** — show what was set up, what needs attention.

**Important:** Import `rich` lazily inside run_setup() — don't require it at import time. Same for `huggingface_hub`. These are only needed during setup, not normal operation.
  </action>
  <verify>
Run `python -c "from vox.setup.launchd import write_plist, bootstrap, bootout, get_status, restart; print('launchd imports OK')"`.

Run `python -c "from vox.setup.permissions import check_accessibility, check_microphone, open_permission_settings, PERMISSION_URLS; print('permissions imports OK'); print(PERMISSION_URLS)"`.

Run `python -c "from vox.setup.wizard import run_setup; print('wizard import OK')"`.

Run `python -c "from vox.setup.launchd import get_status; print(get_status())"` — should show loaded=False if service not running.
  </verify>
  <done>
vox/setup/ package exists with launchd.py (bootstrap/bootout/status/write_plist), permissions.py (check + deep-link for 3 permissions), and wizard.py (interactive 8-step setup including MCP auto-approve writing to ~/.claude/settings.json). All functions importable without heavy dependencies at module level.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire CLI service commands, logs, and TTS interrupt into main loop</name>
  <files>vox/cli.py, vox/main.py</files>
  <action>
**vox/cli.py** — Fill in the stub implementations for service management commands and add setup and logs:

1. `_cmd_start(args)`:
   - Current implementation calls `from vox.main import run; run()`. This is the "direct run" path.
   - Add a `--daemon` flag to the start subparser.
   - If `--daemon`: call `from vox.setup.launchd import bootstrap; success, msg = bootstrap(); print(msg)`.
   - If no `--daemon` (default): keep existing behavior — run the main loop directly (foreground mode, useful for development).

2. `_cmd_stop(args)`:
   - Lazy import `from vox.setup.launchd import bootout`.
   - Call `bootout()`, print result message.

3. `_cmd_restart(args)`:
   - Lazy import `from vox.setup.launchd import restart`.
   - Call `restart()`, print result message.

4. `_cmd_status(args)`:
   - Lazy import `from vox.setup.launchd import get_status`.
   - Call `get_status()`.
   - Print formatted status: "Vox v{version} — {Running (PID {pid}) | Stopped (exit code {code}) | Not installed}".

5. `_cmd_logs(args)` (CLI-01):
   - Run `subprocess.run(["tail", "-f", "/tmp/vox.log"])` (the plist sets StandardOutPath to /tmp/vox.log).
   - Add optional `--lines` / `-n` arg (default 50) passed as `tail -n {lines} -f /tmp/vox.log`.
   - Wrap in try/except KeyboardInterrupt to exit cleanly on Ctrl+C.
   - If `/tmp/vox.log` doesn't exist, print "No log file found. Is the service running?" and exit.

6. `_cmd_setup(args)`:
   - Lazy import `from vox.config import load_config` and `from vox.setup.wizard import run_setup`.
   - Load config, call `run_setup(config)`.

7. Add `--daemon` flag to `start` subparser: `sub_start.add_argument("--daemon", "-d", action="store_true", help="Start as launchd service (background)")`.

8. Add `logs` subparser with `--lines`/`-n` argument (type=int, default=50).

**vox/main.py** — Wire TTS startup, interrupt, and shutdown (TTS-03):

1. At the top of `main()`, after config is loaded and log is initialized:
   - Add: `from vox.audio.tts import start_worker as _start_tts, shutdown as _shutdown_tts, interrupt as _tts_interrupt`.
   - If `config.tts.enabled`: call `_start_tts(config)` to start the TTS background worker.
   - Add to the finally block: if `config.tts.enabled`: call `_shutdown_tts()`.

2. In `start_recording()`:
   - After setting `is_recording = True` and before playing the listening cue:
   - Add: `try: from vox.audio.tts import interrupt as _tts_int; _tts_int(); except ImportError: pass`
   - This immediately pauses any active TTS when the user starts speaking (TTS-03).
   - Use try/except ImportError to gracefully handle case where TTS deps aren't installed.

3. Update the voice command dispatch in `_send_local()`:
   - Currently calls `execute_voice_command(action_key, feedback, tts_script_path=tts_script, log_fn=log)`.
   - Add a check: if `config.tts.enabled` and action_key matches a native TTS command, call the tts module directly:
     - "tts-skip" -> `from vox.audio.tts import skip_current; skip_current()`
     - "tts-stop" -> `from vox.audio.tts import stop_all; stop_all()`
     - "tts-mute" -> `from vox.audio.tts import set_muted, is_muted; set_muted(not is_muted())`
   - Fall through to execute_voice_command() for "tts-next" and "tts-replay" (not yet implemented natively).
   - This means voice commands like "skip" and "stop" now control the native TTS engine when TTS is enabled.
  </action>
  <verify>
Run `vox start --help` — should show `--daemon/-d` flag.

Run `vox status` — should show "Not installed" or actual status (not the Phase 3 stub message).

Run `vox stop` — should call bootout (may show "Not running" if service not loaded).

Run `vox logs --help` — should show `--lines/-n` flag with default 50.

Run `vox setup --help` — should show setup subcommand.

Run `python -c "import ast; tree = ast.parse(open('vox/main.py').read()); print('main.py parses OK')"` — verify no syntax errors.

Grep vox/main.py for `tts.start_worker` or `_start_tts` to confirm TTS worker startup is wired.

Grep vox/main.py for `interrupt` or `_tts_int` in start_recording to confirm TTS-03 interrupt wiring.

Grep vox/main.py for `_shutdown_tts` in finally block to confirm clean TTS shutdown.
  </verify>
  <done>
All seven CLI commands (start with --daemon, stop, restart, status, logs, setup, and existing speak/skip/mute/quiet from Plan 01) are fully implemented. `vox logs` tails /tmp/vox.log with configurable line count. TTS worker starts automatically when vox runs with tts.enabled=True. TTS interrupts immediately when user starts speaking via wake word or PTT. TTS worker shuts down cleanly in main.py finally block. Voice commands (skip, stop, mute) dispatch to native TTS engine when enabled.
  </done>
</task>

</tasks>

<verification>
1. `vox status` — shows service state (loaded/running/not installed)
2. `vox start --daemon` — loads launchd service; `vox status` shows running with PID
3. `vox stop` — unloads service; `vox status` shows not loaded
4. `vox restart` — stops and starts; service running after
5. `vox logs` — tails /tmp/vox.log; `vox logs -n 100` shows last 100 lines
6. `vox setup` — walks through permissions, model download, mic test, MCP auto-approve with rich UI
7. `vox setup` opens correct System Settings pane when permission is missing
8. After `vox setup`, check `~/.claude/settings.json` contains vox MCP server entry
9. Start vox in foreground (`vox start`), then `vox speak "hello"` from another terminal — speech plays, then say wake word — TTS stops immediately (TTS-03)
10. Voice command "skip" while TTS is playing stops current playback
</verification>

<success_criteria>
- launchd service lifecycle works: start/stop/restart/status/logs all functional
- Setup wizard checks 3 permissions with deep-links to System Settings
- Setup downloads Kokoro model with progress indication
- Setup tests microphone with live level display
- Setup writes Vox MCP entry to ~/.claude/settings.json for auto-approve
- TTS worker starts automatically in main loop
- TTS interrupts when recording starts (wake word or PTT)
- TTS worker shuts down cleanly in finally block
- Voice commands dispatch to native TTS engine
</success_criteria>

<output>
After completion, create `.planning/phases/03-cli-tts-output/03-02-SUMMARY.md`
</output>
