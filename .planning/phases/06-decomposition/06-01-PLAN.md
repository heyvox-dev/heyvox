---
phase: 06-decomposition
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - heyvox/app_context.py
  - heyvox/text_processing.py
  - heyvox/main.py
autonomous: true
requirements:
  - DECOMP-04
  - DECOMP-03

must_haves:
  truths:
    - "AppContext dataclass exists with all 17+ former module globals as typed fields"
    - "is_garbled() and strip_wake_words() are importable from heyvox.text_processing"
    - "Existing tests (test_wake_word_strip.py, test_wakeword_trim.py) pass without modification"
    - "HeyVox starts and responds to voice normally after the refactor"
  artifacts:
    - path: "heyvox/app_context.py"
      provides: "AppContext dataclass with all shared state"
      contains: "class AppContext"
      min_lines: 40
    - path: "heyvox/text_processing.py"
      provides: "Pure functions for wake word stripping and garbled detection"
      exports: ["is_garbled", "strip_wake_words", "_WAKE_WORD_PHRASES"]
      min_lines: 80
  key_links:
    - from: "heyvox/main.py"
      to: "heyvox/app_context.py"
      via: "from heyvox.app_context import AppContext"
      pattern: "from heyvox\\.app_context import AppContext"
    - from: "heyvox/main.py"
      to: "heyvox/text_processing.py"
      via: "re-export aliases for backward compat"
      pattern: "from heyvox\\.text_processing import"
---

<objective>
Create the AppContext dataclass and extract text processing pure functions.

Purpose: Establish the shared state container (AppContext) that all subsequent extractions depend on, and extract the lowest-risk code (stateless pure functions) to heyvox/text_processing.py. This is the foundation for Plans 02 and 03.

Output: Two new files (heyvox/app_context.py, heyvox/text_processing.py) and updated heyvox/main.py with re-export aliases. All existing tests pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-decomposition/06-CONTEXT.md
@.planning/phases/06-decomposition/06-RESEARCH.md
@heyvox/main.py
@heyvox/config.py
@heyvox/constants.py
@tests/test_wake_word_strip.py
@tests/test_wakeword_trim.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AppContext dataclass and text_processing.py</name>
  <files>heyvox/app_context.py, heyvox/text_processing.py</files>
  <read_first>
    - heyvox/main.py (lines 1-90 for globals, lines 145-315 for text processing functions)
    - heyvox/config.py (for HeyvoxConfig type reference)
    - heyvox/constants.py (for any constants referenced)
  </read_first>
  <action>
**Create heyvox/app_context.py** (per D-06, D-07):

```python
"""
Shared application context for heyvox.

Replaces 17+ module-level globals in main.py with a single typed dataclass
passed via constructor injection.

Requirements: DECOMP-04
"""
from __future__ import annotations
import dataclasses
import threading
from typing import TYPE_CHECKING

if TYPE_CHECKING:
    from heyvox.hud.ipc import HUDClient
    from heyvox.config import HeyvoxConfig
```

Define `@dataclasses.dataclass class AppContext` with these fields (all migrated from main.py lines 45-87):

Recording state (protected by lock):
- `lock: threading.Lock = dataclasses.field(default_factory=threading.Lock)`
- `is_recording: bool = False`
- `recording_start_time: float = 0.0`
- `busy: bool = False`
- `busy_since: float = 0.0`
- `audio_buffer: list = dataclasses.field(default_factory=list)`
- `triggered_by_ptt: bool = False`
- `recording_target: object = None`
- `cancel_transcription: threading.Event = dataclasses.field(default_factory=threading.Event)`
- `shutdown: threading.Event = dataclasses.field(default_factory=threading.Event)`
- `cancel_requested: threading.Event = dataclasses.field(default_factory=threading.Event)`
- `adapter: object = None`
- `last_inject_time: float = 0.0`
- `inject_lock: threading.Lock = dataclasses.field(default_factory=threading.Lock)`

Device state:
- `consecutive_failed_recordings: int = 0`
- `zombie_mic_reinit: bool = False`
- `last_good_audio_time: float = 0.0`

HUD state:
- `hud_client: HUDClient | None = None`
- `hud_last_reconnect: float = 0.0`
- `hud_last_level_send: float = 0.0`

Process state:
- `indicator_proc: object = None`

IMPORTANT: Every mutable default (list, Lock, Event) MUST use `dataclasses.field(default_factory=...)`. Do NOT use bare `[]` or `threading.Lock()` as defaults.

**Create heyvox/text_processing.py** (per D-03):

```python
"""
Text processing utilities for STT output.

Provides wake word stripping and garbled transcription detection.

Requirements: DECOMP-03
"""
import re
```

Move these from main.py verbatim:
1. `_WAKE_WORD_PHRASES` dict (lines 152-188) — keep the name with underscore prefix since it's a module-internal constant
2. `_is_garbled()` function (lines 191-240) — rename to `is_garbled()` (drop leading underscore, it's now public API). Move `import re` to module level.
3. `_strip_wake_words()` function (lines 243-314) — rename to `strip_wake_words()` (drop leading underscore). Move `import re` to module level.

The functions are pure (no global state, no side effects) so they move without any modifications to their logic. Only changes: drop `_` prefix, remove `import re` from inside function bodies (already at module top).
  </action>
  <verify>
    <automated>cd /Users/work/conductor/workspaces/vox/seattle && python -c "from heyvox.app_context import AppContext; ctx = AppContext(); assert hasattr(ctx, 'lock'); assert hasattr(ctx, 'is_recording'); assert hasattr(ctx, 'shutdown'); print('AppContext OK')" && python -c "from heyvox.text_processing import is_garbled, strip_wake_words, _WAKE_WORD_PHRASES; assert is_garbled('...'); assert not is_garbled('hello world this is a test'); print('text_processing OK')"</automated>
  </verify>
  <acceptance_criteria>
    - heyvox/app_context.py contains `class AppContext`
    - heyvox/app_context.py contains `dataclasses.field(default_factory=threading.Lock)` (not bare `threading.Lock()`)
    - heyvox/app_context.py contains `dataclasses.field(default_factory=list)` (not bare `[]`)
    - heyvox/app_context.py contains `from __future__ import annotations`
    - heyvox/app_context.py contains `TYPE_CHECKING`
    - heyvox/text_processing.py contains `def is_garbled(text: str) -> bool:`
    - heyvox/text_processing.py contains `def strip_wake_words(text: str, start_model: str, stop_model: str) -> str:`
    - heyvox/text_processing.py contains `_WAKE_WORD_PHRASES`
    - heyvox/text_processing.py contains `import re` at module level (not inside functions)
    - heyvox/text_processing.py does NOT contain `import re` inside function bodies
  </acceptance_criteria>
  <done>AppContext dataclass created with all 17+ fields using proper dataclasses.field() for mutable defaults. text_processing.py contains is_garbled(), strip_wake_words(), and _WAKE_WORD_PHRASES as public module API.</done>
</task>

<task type="auto">
  <name>Task 2: Wire main.py re-exports and remove extracted code</name>
  <files>heyvox/main.py</files>
  <read_first>
    - heyvox/main.py (full file — need to see all references to _is_garbled, _strip_wake_words, _WAKE_WORD_PHRASES)
    - tests/test_wake_word_strip.py (line 3: `from heyvox.main import _strip_wake_words`)
    - tests/test_wakeword_trim.py (lines 135-162: imports _audio_rms, _MIN_AUDIO_DBFS from heyvox.main)
    - heyvox/app_context.py (just created — verify it exists)
    - heyvox/text_processing.py (just created — verify exports)
  </read_first>
  <action>
In heyvox/main.py, make these changes:

**1. Add re-export aliases** near the top imports section (after existing imports, before the `# State` section). This ensures `from heyvox.main import _strip_wake_words` still works for existing tests:

```python
# Backward-compat re-exports (tests import these; remove in Phase 9)
from heyvox.text_processing import (
    is_garbled as _is_garbled,
    strip_wake_words as _strip_wake_words,
    _WAKE_WORD_PHRASES,
)
```

**2. Remove the extracted code blocks from main.py:**
- Delete lines 145-314 (the `# Wake word stripping from transcriptions` section): `_WAKE_WORD_PHRASES` dict, `_is_garbled()` function, `_strip_wake_words()` function
- Keep the section separator comment `# Wake word stripping` if desired, but replace the body with a comment: `# Moved to heyvox/text_processing.py — re-exported above for backward compat`

**3. Verify all call sites in main.py** that reference `_is_garbled` and `_strip_wake_words` still work via the re-export aliases. These call sites are in `_send_local()` (around line 794+). The re-export aliases make them available as module-level names, so no call-site changes needed.

**4. Do NOT yet migrate globals to AppContext** in this task — that happens in Plans 02 and 03. The globals remain as-is in main.py for now. AppContext is created but not yet wired.

**5. Run the full test suite** to verify nothing broke.
  </action>
  <verify>
    <automated>cd /Users/work/conductor/workspaces/vox/seattle && python -m pytest tests/test_wake_word_strip.py tests/test_wakeword_trim.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - heyvox/main.py contains `from heyvox.text_processing import` (re-export line)
    - heyvox/main.py contains `is_garbled as _is_garbled`
    - heyvox/main.py contains `strip_wake_words as _strip_wake_words`
    - heyvox/main.py does NOT contain `def _is_garbled(text: str)` (function definition removed)
    - heyvox/main.py does NOT contain `def _strip_wake_words(text: str` (function definition removed)
    - heyvox/main.py does NOT contain the `_WAKE_WORD_PHRASES` dict definition (only the re-import)
    - pytest tests/test_wake_word_strip.py exits 0
    - pytest tests/test_wakeword_trim.py exits 0
  </acceptance_criteria>
  <done>Text processing code removed from main.py, re-export aliases in place, all existing tests pass. main.py reduced by ~170 lines.</done>
</task>

</tasks>

<verification>
1. `python -c "from heyvox.app_context import AppContext; ctx = AppContext(); print(type(ctx.lock), type(ctx.shutdown))"` — prints Lock and Event types
2. `python -c "from heyvox.text_processing import is_garbled, strip_wake_words; print('OK')"` — imports succeed
3. `python -c "from heyvox.main import _is_garbled, _strip_wake_words; print('re-exports OK')"` — backward compat works
4. `python -m pytest tests/test_wake_word_strip.py tests/test_wakeword_trim.py -x -q` — all tests pass
5. `grep -c "def _is_garbled" heyvox/main.py` — returns 0 (function removed)
6. `grep -c "def is_garbled" heyvox/text_processing.py` — returns 1 (function exists)
</verification>

<success_criteria>
- heyvox/app_context.py exists with AppContext dataclass (all 17+ fields, proper defaults)
- heyvox/text_processing.py exists with is_garbled(), strip_wake_words(), _WAKE_WORD_PHRASES
- heyvox/main.py has re-export aliases for backward compat
- heyvox/main.py no longer contains _is_garbled, _strip_wake_words, _WAKE_WORD_PHRASES definitions
- All existing tests pass (especially test_wake_word_strip.py and test_wakeword_trim.py)
- main.py reduced by ~170 lines
</success_criteria>

<output>
After completion, create `.planning/phases/06-decomposition/06-01-SUMMARY.md`
</output>
