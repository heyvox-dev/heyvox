# HeyVox Defect Log

## DEF-163 — Keepalive PA context freezes silently: cues stop playing, no auto-recovery

- **Date**: 2026-06-29
- **Category**: state-pollution / platform
- **Severity**: S2 (cues completely silent; voice input still works)
- **Symptom**: "listening" and "ok" audio cues stop playing. afplay fallback also silent because G535 Lightspeed parks the output path after a short silence (DEF-150 original problem). User notices only after several minutes.
- **Root cause**: OutputKeepAlive._open_stream() drops and recreates the PortAudio context on every failure (DEF-153 fix). But if the USB device flaps and the PA context stays stale indefinitely, _open_fail reaches 228+ with only a log line at attempt 24. No auto-recovery: the log says "daemon restart required" but nothing actually triggers one.
- **Fix**: Added `stale = threading.Event()` to OutputKeepAlive, set after 24 consecutive failures. Main-loop watchdog (runs every ~10s) checks `keepalive.stale.is_set()` and triggers `os.execv()` restart with a HUD banner, matching the memory-watchdog pattern.
- **Found by**: User noticed missing cues. Log showed `[keepalive] could not open silent stream (attempt 228...)`.
- **Would have caught earlier**: A metric/alert on `_open_fails >= 24` rather than a log-only message. Auto-restart at that threshold (what we're now adding) prevents the silent degradation from ever reaching 228+.

## DEF-164 — preload_model() no-op leaves a stale idle-unload timer armed, evicting a warm model mid-recording

- **Date**: 2026-07-02
- **Category**: race / timing
- **Severity**: S3 (STT still works, but pays a full un-hidden cold-reload — several seconds — right when the user is waiting for a response)
- **Symptom**: User reported "transcription is slow again, it used to be much faster." `heyvox log-health` showed STT p50=1.08s (fine) but p99=10.29s with a fat tail. Two of four cold loads (`stt_warm=false`) that day had no plausible explanation as genuine >5min-idle cold starts — one followed a 12.96s recording, the other a 53.92s recording, both far longer than the model's own load time.
- **Root cause**: `preload_model()` (`heyvox/audio/stt.py`) is correctly called at the start of every recording to hide model-load latency behind speaking time, but its already-loaded fast path was a bare `if _mlx_loaded.is_set(): return` — it never touched `_mlx_last_use` or rescheduled the idle-unload `threading.Timer`. That timer was armed 300s (`unload_secs`) after the *previous* transcription finished, independent of the new recording. If the new recording ran long enough to cross that pre-existing deadline, `_unload_mlx_model()` fired mid-recording (`_mlx_transcribing` is False until `transcribe_audio()` actually runs, so its guard doesn't block this), evicting a model that was warm and in active use. Confirmed via exact log lines in `/tmp/heyvox.log.1`: `[16:57:53] MLX model loaded in 4.9s` → `[17:02:58] MLX model unloaded after 300s idle` → `[17:03:04] TRIGGER / Stopping recording` (a NEW recording that had started ~17:02:49, 6s before the stale timer fired) → forced fresh reload with zero head start.
- **Fix**: `preload_model()`'s already-loaded branch now refreshes `_mlx_last_use = time.time()` and calls `_schedule_unload()` under `_mlx_lock`, the same way the end of `transcribe_audio()` already does — treating "a new recording started" as a use-signal, not just "a transcription completed." Commit touches `heyvox/audio/stt.py` (behavior) and `heyvox/recording.py` (stale "~1s" comment corrected to match the current model's actual load time).
- **Found by**: User asked "shouldn't reload hide behind recording start, so mostly unnoticeable?" while reviewing the STT-latency investigation — verifying that claim against the two actual cold-load log entries surfaced the timer race.
- **Would have caught earlier**: No test coverage existed for `preload_model()` / the idle-unload timer interaction at all (`grep -rl preload_model tests/` was empty before this fix). Added `test_def164_preload_model_refreshes_idle_timer_when_already_loaded` + a not-loaded counterpart to `tests/test_defect_guards.py`. General pattern: any module-level timer/last-use pair needs a test asserting *every* code path that "uses" the resource also refreshes it — not just the obvious one (transcription completion), but adjacent ones (preload-into-already-warm).

## DEF-165 — Unbounded NSPasteboard call freezes main loop for ~3 minutes after paste

- **Date**: 2026-07-02
- **Category**: timing / error-handling
- **Severity**: S1 (whole voice pipeline wedged; PTT event tap starved; process needed a full manual restart)
- **Symptom**: User reported HeyVox "super slow", possibly stuck processes. Log showed `WARNING: busy flag stuck for 60.0s, force-resetting (watchdog)`, but the freeze didn't actually clear — 46× `CGEventTap was disabled by macOS, re-enabled` followed over the next ~2 minutes (macOS repeatedly killing the PTT event tap because its callback thread never returned). Process was eventually killed and restarted manually; the next start logged `Previous instance died without clean shutdown (heartbeat stale by 61s)`.
- **Root cause**: `app_fast_paste()` (`heyvox/input/injection.py`) calls `_set_clipboard()` then `get_clipboard_text()` — raw PyObjC `NSPasteboard` calls — with no timeout. Every other blocking call in that function (both osascript `subprocess.run` calls) already has an explicit timeout (2s / 7s); these two didn't, because Python has no built-in way to bound a blocking native/ObjC call. When the pasteboard server briefly wedged, the calling (and only) thread blocked indefinitely — the busy-flag watchdog can reset a *flag*, but can't unstick the blocked thread producing it. Pinpointed by process-of-elimination in `recording.py` (the paste-outcome log line fires, then nothing — no verify/"Sent!"/fail line for that cycle — and the only statement between those two log points is the `app_fast_paste()` call), then confirmed directly in the rotated log (see DEF-166): `app_fast_paste: profile=Conductor ... text_len=360` is the last line before the watchdog fires; the function's own completion log (`[TIMING] app_fast_paste: OK ...`) never appears for that call.
- **Fix**: Added `_call_with_timeout()` to `heyvox/input/injection.py` — runs the AppKit call on a throwaway daemon thread and `.join(timeout=2.0)`, raising `TimeoutError` if it hasn't returned (the thread is abandoned, not killed — Python can't interrupt a native call — but a fresh thread per call means one abandoned call can't poison later ones). Applied to all 4 untimed AppKit call sites: `_set_clipboard`, `get_clipboard_text`, `_clipboard_still_ours`, `_verify_target_focused`.
- **Found by**: User reported perceived slowness (already manually restarted before root-causing). Diagnosed from `/tmp/heyvox.log` plus the rotated `/tmp/heyvox.log.1` — see DEF-166, the incident-window content was only visible in the rotated file.
- **Would have caught earlier**: A per-call timeout (this fix) converts what used to be an unbounded hang into a bounded, immediately-loggable failure the first time it happens, instead of requiring a live ~3-minute incident and post-hoc log archaeology to notice. General pattern: any blocking call inside a single-threaded event loop needs an enforced upper bound — `subprocess.run(timeout=...)` already gets this by convention in this codebase, but PyObjC/AppKit calls were assumed "fast enough" and never audited the same way.

## DEF-166 — injection.py/target.py `_log()` output silently lost after first log rotation

- **Date**: 2026-07-02
- **Category**: config / dead-code (logging split-brain)
- **Severity**: S2 (no functional impact on the app, but directly extended the DEF-165 investigation — the exact hang line was invisible for most of the effort)
- **Symptom**: `heyvox/input/injection.py`'s `_log()` (and `heyvox/input/target.py`'s `_log()`) never appeared in `/tmp/heyvox.log` — zero occurrences, ever, despite both being called on every single paste (`grep -c "app_fast_paste" /tmp/heyvox.log` → 0 across 7400+ lines spanning days of normal use).
- **Root cause**: `main.py`'s central `log()` rotates by `os.replace(_LOG_FILE, _LOG_FILE + ".1")` once the file exceeds 1MB, then reopens `_LOG_FILE` by path (a fresh `open()` naturally finds the new inode). `injection.py`/`target.py`'s `_log()` instead did `print(msg, file=sys.stderr, flush=True)`, relying on launchd's `StandardErrorPath` fd redirect — fixed to the inode that was open at process start. `os.replace()` repoints the *path*, not fds opened before the rename, so every stderr write after the first rotation silently landed in the renamed-away (orphaned) inode for the rest of the process's life, invisible from the live log. Confirmed: the "missing" lines were sitting, fully intact, in `/tmp/heyvox.log.1` (`grep -c "\[injection\]" /tmp/heyvox.log.1` → 258).
- **Fix**: Rewrote both modules' `_log()` to reopen the config-resolved path on every call (`open(path, "a")`), matching the already-established `main.py` / `heyvox/audio/media.py` (DEF-126) pattern. The path string is resolved once and cached (`_resolve_log_path()`) rather than calling `load_config()` per `_log()` call — an initial version re-resolved every call and regressed `_ax_inject_text`'s hot path from <5ms to >10ms mean, failing `test_ax_inject_text_phase12_fastpath_remains_under_5ms`.
- **Found by**: While root-causing DEF-165 — had to grep the *rotated* `.log.1` file to find the incident-window `app_fast_paste` entry line at all; it does not exist anywhere in the live file.
- **Would have caught earlier**: No user-visible symptom on its own, so nothing would have surfaced it directly — but it's the second occurrence of the same root cause first named for DEF-126 (see Patterns below). `heyvox/audio/mic.py`, `heyvox/input/ptt.py`, `heyvox/input/toast.py`, and `heyvox/adapters/conductor.py` all still use the same bare `print(file=sys.stderr)` pattern and are equally exposed after the process's first log rotation; not fixed here (out of scope for this incident) — flagged as a follow-up in Patterns.

## DEF-167 — Retroactive TrainingCollector relabelers manufactured mislabeled wake-word training data

- **Date**: 2026-07-02
- **Category**: state-pollution / error-handling
- **Severity**: S2 (does not crash the app or lose user-facing functionality, but silently degrades the wake-word model's own training data over time — every retrain compounds the mislabeling)
- **Symptom**: During a routine training-data audit this session, a resumable Whisper pass (`REFERENCE-fp_check_step0.py`, absorbed into `tools/quality_gate.py` by this same plan) was run over `fp/` (304 clips at the time) to check how many contained the actual wake word. It found 22 of 192 previously-unaudited fp-reclassified clips (11.5%) DID contain "Hey Vox" (including box/fox/vocks-style Whisper garbles of the user's real voice) — meaning `reclassify_tp_start_as_fp` had moved 22 genuine wake-word triggers into the false-positive training set. A companion pass over `fn_start`-reclassified clips found 54% contained NO wake word at all — meaning `reclassify_fn_start` had moved ordinary background-noise TN clips into the false-negative training set purely because a trigger happened to follow within a 5-second window, with zero evidence the TN clip itself contained speech.
- **Root cause**: Two methods on `TrainingCollector` (`heyvox/audio/training_collector.py`) performed RETROACTIVE, evidence-free relabeling based on temporal proximity/outcome heuristics rather than STT evidence: `reclassify_tp_start_as_fp(reason)` moved the most recent `tp_start` clip to `fp/` any time a trigger's recording aborted (no speech, empty STT, user-cancelled, or post-trim-too-short) — but "the recording aborted" does not prove "the wake word wasn't actually said" (the user could say "Hey Vox" correctly and then hesitate, get interrupted, or have the mic clip the recording for unrelated reasons). `reclassify_fn_start()` moved any `tn/` clip saved within the prior 5 seconds to `fn/` whenever a SUCCESSFUL trigger followed — but "a trigger followed a hard-negative sample" does not prove the hard-negative clip itself contained a missed wake word; it just means the user spoke again shortly after, for any reason. Both mechanisms manufacture training labels from correlation/timing instead of the transcript evidence that `classify_stop_outcome()` (the one relabeling path that stayed, `save_fn_stop`) already correctly required. Net effect: every `reclassify_*` call polluted the training set with clips whose true content contradicted their assigned label, and both pollution modes push the model toward FALSE NEGATIVES on the user's own voice (real positives got labeled fp; real hard-negatives got labeled fn, i.e. "should have fired here" on audio where no wake word was said).
- **Fix**: Deleted `reclassify_tp_start_as_fp()` and `reclassify_fn_start()` outright, along with the `_recent_tp_start`/`_recent_tn` tracking lists and the `fn_reclassify_window_secs` constructor param that existed solely to feed them, and removed all six call sites across `heyvox/recording.py` and `heyvox/main.py`. `save_fn_stop()` (evidence-based via `classify_stop_outcome()`, unchanged) remains the only automatic fn-labeling path from the live daemon. In its place, built a MANDATORY batch Whisper quality gate (`tools/quality_gate.py`, wired into `tools/collect_personal_features.py` ahead of every featurization run) that re-derives BOTH directions of evidence-based correction from the transcript itself: any positive-dir clip without the wake word is quarantined; any negative-dir clip WITH the wake word is recovered into `positives/` (short clips) or `quarantine/` (long clips, likely contains more than just the wake word) — this is the new evidence-based fn-recovery mechanism, replacing the deleted heuristic one, but gated on STT proof rather than timing correlation. Schritt-0 cleanup already moved 26 clips out of `fp/` manually before this fix landed (22 to `positives/`, 4 to `quarantine/`; manifest: `recfp_cleanup_manifest_20260702_084240.json`) — that hand-run pass is what the automated gate in this plan now performs on every collect run.
- **Found by**: This session's manual investigation, starting from a Whisper sanity pass over `fn/` + a backed-up `fp_garbled/` subset (`tools/whisper_sanity_pass.py`, pre-existing) that had never been run over the full `fp/` directory or the `fn_start`-reclassified clips specifically — extending that audit to ALL of `fp/` (not just the `fp_garbled` subtype) via the more robust `REFERENCE-fp_check_step0.py` driver surfaced the 22-clip contamination; a parallel audit of `fn_start` clips surfaced the 54% figure.
- **Would have caught earlier**: A MANDATORY Whisper gate over ALL training-data directories (this plan's Task 2/3) run automatically before every featurization, rather than an ad-hoc manual audit script that only ever covered a subset of one category (`fn/` and a backed-up `fp_garbled/` sample) and had to be manually re-scoped and re-run this session to find the gap. General pattern: any heuristic/retroactive relabeling of ALREADY-COLLECTED evidence-backed data (a saved audio clip is direct evidence; a temporal correlation is not) should never ship without either (a) requiring the same evidence standard as the mechanism it's "correcting," or (b) being paired with a permanent, mandatory audit step — not a one-off script run manually after the fact once contamination is suspected.

## DEF-168 — Escape-stops-TTS silently dead under system load: CGEventTap re-enable only polled at 1Hz, not reactive

- **Date**: 2026-07-02
- **Category**: timing / platform
- **Severity**: S2 (Escape-cancel and PTT gestures become unreliable to effectively non-functional under sustained high CPU load; rest of the voice pipeline — wake word, STT, TTS playback itself — keeps working; self-heals once load drops, no restart required)
- **Symptom**: User reported "esc doesnt stop tts anymore." Live `/tmp/heyvox.log` showed the tap health watchdog logging `WARNING: CGEventTap was disabled by macOS, re-enabled (#N)` essentially every 1-2s, continuously, starting 09:26:57 and still ongoing at investigation time (09:34, #386+ and counting; 537 disables in the post-08:36-restart window alone). Cross-referencing `Escape:` log lines showed the mechanism worked 3 times right after an 08:36 restart (08:38:11, 08:43:16, 09:08:31) but zero times since — i.e. Escape presses stopped being captured at all once the flapping became continuous, not merely handled-incorrectly.
- **Root cause**: `start_ptt_listener()` (`heyvox/input/ptt.py`) creates a `CGEventTapCreate` tap and relies entirely on `_tap_watchdog()` — a `time.sleep(1.0)` polling loop — to notice `CGEventTapIsEnabled(tap) == False` and call `CGEventTapEnable(tap, True)`. macOS actually delivers a dedicated notification event (`kCGEventTapDisabledByTimeout` / `kCGEventTapDisabledByUserInput`) to the tap callback itself the instant it disables a tap — Apple's own sample code re-enables synchronously right there — but `_callback_inner()` never checked for these event types, falling through to a bare `return event`. Under system-level CPU contention (observed load average 7.30 on an 8-core machine — consistent with multiple parallel Conductor/Claude agent sessions plus browser renderers, matching [[user_parallel_conductor_sessions]]), macOS was disabling the tap faster than the 1Hz poll could re-enable it, so the effective "listening" window per second shrank to near zero — any Escape press landing in a disabled window was dropped entirely, not mishandled by the `is_busy`/`is_recording`/`is_speaking` branch logic (which is correct and was not the problem — traced `on_cancel_tts` → `_stop_tts_from_escape` → `heyvox.audio.tts.stop_all()` → `heyvox.herald.cli stop` → `_kill_afplay()`+`_clear_tts_state()`+queue purge, all intact).
- **Fix**: Added an immediate in-callback check in `_callback_inner()` (`heyvox/input/ptt.py`) for `Quartz.kCGEventTapDisabledByTimeout` / `Quartz.kCGEventTapDisabledByUserInput` that calls `CGEventTapEnable(tap, True)` and logs, before any other event handling. This doesn't eliminate OS-level starvation under extreme load, but removes the up-to-1000ms self-inflicted lag that made the disabled window compound faster than recovery — the poll-based `_tap_watchdog` remains as a redundant safety net. Restarted via `launchctl kickstart` to apply.
- **Found by**: User's live bug report, root-caused by reading `/tmp/heyvox.log` in real time during the same session (the flapping was actively ongoing at investigation time, not reconstructed after the fact) and cross-referencing timestamps of successful vs. absent `Escape:` log lines against the disable-warning stream.
- **Would have caught earlier**: No test coverage exists for `start_ptt_listener`'s Quartz callback at all — `tests/test_ptt_gesture.py` explicitly scopes itself to the Quartz-free `GestureRecognizer` FSM only ("no real threads or Quartz are involved" by design), and DEF-087/DEF-088 (the original tap-watchdog work) shipped without a mock-Quartz harness either. General pattern: this is the second incident (after DEF-165) where the *symptom* was "CGEventTap flapping," but the two have different root causes (unbounded blocking call vs. missing reactive re-enable) — a synthetic load-test (spin all cores, assert tap re-enable latency stays sub-100ms) would catch both failure classes without needing a full Quartz mock.

## DEF-169 — Garbled-discard recovery pointer (raw WAV → large-v3 reverify) was dead code

- **Date**: 2026-07-02
- **Category**: dead-code / error-handling
- **Severity**: S2 (no crash; the large-v3 reverify-and-clipboard safety net for a discarded transcription silently never ran, for any user, regardless of settings — the discarded text itself is only recoverable by hand from `heyvox-debug/` if debug capture happens to be on)
- **Symptom**: While investigating a user report of "wake word fires, transcription gets thrown away" plus generally slow STT, `/tmp/heyvox.log` + `.log.1` showed 5 `FILTER (garbled, stt=...)` discards in a ~13.5h window, including one on a real, coherent, complete German sentence discarded purely because STT took 11.8s for 14.6s of audio (catastrophic-ratio guard, `text_processing.py`, unrelated to this defect). None of the 5 discards logged "raw audio preserved at ..." or "raw audio captured on-demand for reverify at ..." despite `recording.py` clearly containing that code path, and the HUD showed "Garbled STT - try again" every time instead of "reverifying with large-v3..." — meaning the existing large-v3 background-reverify-and-clipboard-copy feature (`_reverify_garbled_async`) had never actually fired for this user, on a machine where `STT_DEBUG_DIR` was active and the raw/trimmed WAVs for every one of the 5 incidents were confirmed sitting on disk.
- **Root cause**: Two independent, compounding bugs in `heyvox/recording.py`'s local-STT path. (1) `_last_raw_wav` is set inside `stop()` (the method that saves debug audio and dispatches transcription) but read back inside `_send_local` — a *different* method, run on its own daemon thread per its own comment ("_send_local runs on a daemon thread and must not read ctx fields that could be overwritten"). `_last_raw_wav` was never passed as a parameter or stored on `self`/`ctx`, so referencing it in `_send_local` was a guaranteed `NameError` on every single call, silently swallowed by a bare `except NameError: pass`. (2) The on-demand fallback for when debug capture is off (`if _raw_for_reverify is None and audio_chunks: ...` — persists *this* garbled recording to a temp WAV so reverify can still run) checked `audio_chunks` *after* `audio_chunks.clear()` had already emptied that exact list a few lines earlier (`# Free audio chunks immediately — no longer needed after transcription`), so it always saw an empty list and never fired either. Net effect: the recovery/reverify pointer was 100% dead code in every configuration, not just the "debug capture off" case the code comments anticipated.
- **Fix**: Added `raw_wav_path: str | None = None` to `_send_local`'s signature and threaded `_last_raw_wav` through `stop()`'s `threading.Thread(..., kwargs={...})` dispatch so it actually crosses the method/thread boundary. Replaced the `try/except NameError` lookup with a direct `_raw_for_reverify = raw_wav_path` read. Moved `audio_chunks.clear()` from immediately-after-transcription to two call sites — right after the on-demand-fallback save attempt (garbled branch) and right after the garbled-check block (non-garbled branch) — so the fallback can see real audio data in both the "debug capture on" and "debug capture off" cases, while still freeing the buffer at the earliest point each branch no longer needs it (no behavior change for the common non-garbled path). `heyvox/recording.py` only; `text_processing.py`'s ratio-guard itself is untouched — that's a separate, deliberately-not-yet-changed issue (re-tuning it was flagged as the wrong first move; the actual STT-slowness root cause is still open).
- **Found by**: User asked for a log review of a "wake word not ignored + transcription discarded" + "STT is very slow" report. Cross-referencing the 5 `FILTER (garbled)` log lines against `heyvox-debug/` showed the raw/trimmed WAVs existed on disk for every incident even though the log never mentioned them — the discrepancy between "file demonstrably exists" and "code path that's supposed to say so never logs" led directly to the two dead-code sites.
- **Would have caught earlier**: No test exercised `_send_local`'s garbled-discard branch at all (`grep -rl raw_wav_path\|_last_raw_wav tests/` was empty before this fix) — the NameError-swallow and the clear()-ordering bug both hide in exactly the kind of place only an explicit test looks: a rare (garbled), silently-caught-exception (bare `except: pass`) branch. Added `test_stop_passes_raw_wav_path_kwarg_to_send_local`, `test_send_local_garbled_with_debug_wav_triggers_reverify`, and `test_send_local_garbled_without_debug_wav_falls_back_on_demand` to `tests/test_recording_state.py`; confirmed all three fail against the pre-fix code (via `git stash`) before confirming they pass against the fix. General pattern, adjacent to P-log-path-split below: a bare `except SomeSpecificError: pass` around a variable *reference* (not a fallible operation) is almost always masking a scoping bug, not handling a real runtime condition — worth a second look wherever this shape appears.

## DEF-170 — STT-slowness system-contention root cause had no automatic log signal (process gap, not a crash)

- **Date**: 2026-07-02
- **Category**: dead-code / config (observability gap)
- **Severity**: S3 (no functional impact; closes a process gap that made a real user-facing symptom — slow STT — expensive to root-cause)
- **Symptom**: Following the DEF-169 investigation, the user asked to tackle the still-open item: *why* STT decode occasionally takes 10-20x its own warm baseline (5 of 40 sampled completions in the prior investigation exceeded 10s, two exceeded 20s, `warm=True` in every case — not a cold-reload story). The prior investigation could only correlate this with system memory pressure *after the fact*, by manually cross-referencing `[RAM_PRESSURE]` log lines against STT timing lines, and even then only explained one of two slow-STT clusters — the other showed moderate RAM (2100-2700MB) with no obvious excuse.
- **Root cause**: No log line captured system contention *at the moment* of a slow STT completion — `heyvox/ram_pressure.py` already existed and periodically banners on RAM pressure, but (a) it polls independently on its own ~60s cadence, not synchronized with individual STT calls, and (b) it only measures RAM, not CPU. Checking the live machine mid-investigation (`uptime`, `ps aux -r`) found load averages of 13.82 and later 32.2 on an 8-core M1 Pro, with 4+ concurrent `claude` agent processes plus Conductor's own WebKit/WindowServer overhead visible in `ps` — consistent with [[user_parallel_conductor_sessions]] (Franz routinely runs 3-5 parallel Conductor/Claude sessions) and the same load-average-oversubscription class already named in DEF-168 (CGEventTap starvation, load avg 7.30 that time). RAM pressure alone does not capture this: at the moment of the `load1=32.2` reading in this session, `avail_mb=7946` and `pressure=normal` — i.e. the machine was clearly CPU-oversubscribed while looking completely healthy on the RAM-only signal. This is very likely the same underlying mechanism as the STT slowness (MLX Whisper inference competing for CPU/GPU scheduling and unified-memory bandwidth against other heavy processes), but is now a strongly-supported live-evidence correlation, not a proven per-historical-incident causation.
- **Fix**: Added `heyvox.ram_pressure.contention_snapshot()` — a small, defensive (never-raises, each field independently degrades to `"?"`) function returning `avail_mb=N pressure=<normal|warn|critical|?> load1=N` — and wired it into the existing `[TIMING] stop→STT done` log line in `heyvox/recording.py` so every single STT completion (not just slow ones) now carries a synchronized system-contention snapshot. This is instrumentation only: it does not change STT behavior, does not touch the `is_garbled` ratio threshold, and does not attempt to mitigate the contention (e.g. thread priority, adaptive model fallback) — those remain open, deliberately deferred pending more data from this signal and a separate discussion, since the two obvious levers (re-tuning a ratio guard that's already been tuned once; asking Franz to run fewer parallel sessions) both have real downsides raised in the prior investigation.
- **Found by**: Live system inspection (`uptime`, `ps aux -r`, `memory_pressure`) during this session, cross-checked against the already-existing but RAM-only `heyvox/ram_pressure.py` module and its docstring (which documents cold-reload latency from RAM pressure, a *different and less severe* symptom than the warm-inference 10-20x slowdown actually observed).
- **Would have caught earlier**: This entry *is* the "would have caught earlier" for the DEF-169 investigation — the STT timing log now carries the context needed to confirm or rule out system contention on sight, for every future slow-STT report, without re-deriving it by hand. General pattern: a periodic/independent monitor (ram_pressure's 60s poll) is not a substitute for a synchronized snapshot at the exact moment of the event you're trying to explain — the two serve different purposes (background banner vs. point-in-time correlation) and this codebase only had the former for STT latency investigations.

## DEF-171 — MLX/sherpa transcription worker thread ran at default QoS despite the machine being routinely CPU-oversubscribed

- **Date**: 2026-07-02
- **Category**: timing / platform
- **Severity**: S3 (experimental mitigation for the DEF-169/DEF-170 slow-STT investigation, not a confirmed fix — explicitly shipped without automated test coverage or live A/B verification at the user's request)
- **Symptom**: Continuation of the same-day STT-slowness investigation (DEF-169, DEF-170). With `contention_snapshot()` now live, this session confirmed load averages of 13.82 and 32.2 on an 8-core M1 Pro at moments unrelated to any single STT call — i.e. the machine is routinely oversubscribed by parallel Conductor/Claude sessions, not just during rare spikes.
- **Root cause**: `transcribe_audio()`'s `ThreadPoolExecutor` worker (`heyvox/audio/stt.py`, both the MLX and sherpa branches) ran `mlx_whisper.transcribe(...)` / the sherpa decode loop at whatever QoS class Python's default `threading.Thread` gets — no different from any other background thread on the machine, competing on equal footing with WebKit renderers, other `claude` processes, etc. for the same 8 cores. Under genuine oversubscription (load average > core count), the scheduler has no signal that this particular thread is latency-sensitive foreground work the user is actively waiting on.
- **Fix**: Added `_boost_transcribe_thread_priority()` (`heyvox/audio/stt.py`) — a ctypes call to `pthread_set_qos_class_self_np(QOS_CLASS_USER_INITIATED, 0)`, invoked as the first line of the actual worker callable in both the MLX path (new `_run_mlx_transcribe` wrapper around `mlx_whisper.transcribe`) and the sherpa path (`_sherpa_transcribe`). Deliberately `USER_INITIATED`, not the top `USER_INTERACTIVE` tier — that tier is reserved for UI-blocking work, and this codebase already has a thread that needs to win there under contention: the PTT CGEventTap callback (DEF-168 fixed a starvation failure mode for that exact thread under load averages of 7.30). Boosting STT to the *same* top tier could reintroduce that failure mode from the other direction; `USER_INITIATED` still outranks the unset default without contesting the tier PTT needs.
- **Verification**: Explicitly shipped **without automated regression tests or a live PTT-under-load A/B check**, at the user's request ("ok, mal ohne testen") — the tradeoff was discussed first (see DEF-170's closing exchange) and accepted knowingly. Manual smoke-check only: `_boost_transcribe_thread_priority()` called standalone (no exception), and a full `init_local_stt` + `transcribe_audio` round-trip against the real cached `whisper-large-v3-turbo` model (model load 3.0s, transcribed silence to the expected `"Vielen Dank."` DEF-137 artifact, no errors). Full existing suite stayed green (1028 passed/6 skipped) — confirms no import/syntax/behavioral regression in what's already covered, but does NOT confirm the QoS change achieves its intended effect (faster STT under contention) or that it doesn't degrade PTT responsiveness in practice.
- **Found by**: Direct continuation of the DEF-170 investigation at the user's explicit instruction to try the thread-priority lever.
- **Would have caught earlier**: N/A — this is a same-session experimental change, not a regression. **Follow-up needed**: `contention_snapshot()` (DEF-170) now rides on every STT log line specifically so this can be evaluated with real data — watch for (a) whether `load1`-correlated STT slow-tail outliers shrink after this ships, and (b) any recurrence of DEF-168-style `CGEventTap was disabled` flapping correlated with active transcription, which would mean `USER_INITIATED` still contends too much and the PTT thread needs its own explicit QoS floor rather than relying on relative default-vs-boosted weighting.

## DEF-172 — Cue playback (listening/ok) silently routed to the wrong output device while TTS routed correctly

- **Date**: 2026-07-03
- **Category**: platform / error-handling
- **Severity**: S2 (audible feedback cues effectively inaudible to a headset-wearing user — they play, just not where the user can hear them; voice input, wake word, and TTS speech all keep working correctly)
- **Symptom**: User reported that "listening"/"ok" cues played through the MacBook's internal speaker while TTS speech played correctly through the G435 headset, at the same time, both macOS System Settings and HeyVox's own reported output device showing the headset as active. This followed a separate incident earlier the same day where the G435's USB/Lightspeed dongle flickered for several minutes (confirmed via `coreaudiod`'s own `AudioAnalytics` telemetry showing repeated `output_device_source_list: Internal Speaker` and the USB audio engine's `IOWorkLoopInit`/`IOWorkLoopDeinit` cycling every few seconds), which independently caused a total mic outage (cooldown-storm on all input devices, fixed by a daemon restart) and a transient TTS misroute (self-resolved once the flapping stopped, since `afplay` re-resolves fresh per call).
- **Root cause**: `OutputKeepAlive._open_stream()` (`heyvox/audio/keepalive.py`) opened its PyAudio output stream via `pa.open(output=True, ...)` with no `output_device_index` — relying entirely on PortAudio's own internal default-output resolution rather than the live CoreAudio default that `default_output_transport()` (same file, two lines earlier) had just verified via direct ctypes. `cues.py`'s `_play_via_sounddevice()` fallback had the identical gap: `sounddevice.play(data, samplerate)` with no `device=`. Both mechanisms can silently diverge from the live CoreAudio default after device churn — confirmed empirically: a brand-new process's PyAudio default resolved correctly to the G435 at investigation time, but the *already-open* keep-alive stream (opened once, hours earlier) had no mechanism to notice if the underlying device object had since been swapped by a reconnect that didn't change the transport type (USB → USB), since the monitor loop (`_loop()`, 5s poll) only reacts to *transport-type* flips, not same-transport object churn. Once such a stream opens "successfully" against a stale-but-still-valid device, `play_cue()` only checks `self._stream is not None` — no exception is ever thrown, so DEF-153's failure-triggered context-drop-and-recreate never fires. This is a distinct failure mode from DEF-104/148/150/153 (which all recover from *loud* failures) — here nothing fails, it's just silently bound to the wrong device. TTS never hits this class of bug at all: `orchestrator.py` spawns a fresh `afplay` OS process per utterance, which re-resolves CoreAudio's live default every single time, immune to any process-lifetime caching.
- **Fix**: Added `get_default_output_name()` to `heyvox/audio/output.py` (thin wrapper over the module's existing, already-correct `get_default_output_id()` + `_get_device_name()`). `keepalive.py`'s `_open_stream()` now resolves the matching PyAudio device index (by name + `maxOutputChannels > 0`, to disambiguate the G435's separate input/output sub-devices which share one name) via a new `_resolve_output_index()` and passes it explicitly as `output_device_index=`; it also now re-resolves on *every* monitor tick (not just on open) and recycles the stream (close + reopen against the new index) if the target index changed while `self._stream` was already set — closing the same-transport silent-churn gap, not just the open-time binding. `cues.py`'s `_play_via_sounddevice()` gained the equivalent `_resolve_sounddevice_output_index()` matching against `sounddevice.query_devices()`. Both resolvers degrade to `None` on any failure (missing method, device vanished mid-lookup, etc.), which both `pyaudio.Stream.__init__` and `sounddevice.play()` document as behaving identically to omitting the parameter (verified against the installed packages' own docstrings, not assumed) — so today's original behavior is the fallback, never a hard failure. Also improved the `[keepalive]` open-success log line to record the resolved `device_index=`, closing a diagnostic gap that made this incident require live `coreaudiod` unified-log forensics to root-cause instead of a one-line log read.
- **Found by**: User's live report during the same session as the G435 flicker investigation, immediately after being told the flicker was already resolved — the cue/TTS device split was still actively reproducing at report time (unlike the earlier transient misroute), which is what made it root-causeable rather than another one-off.
- **Would have caught earlier**: No test asserted that cue playback and TTS playback resolve to the *same* output device — `test_def148_keepalive_opens_one_stream_and_closes` and `test_def153_...` both stub `FakePA.open(**kw)` without ever inspecting *which* device index was requested, so a missing/wrong `output_device_index` would pass silently. General pattern (ties into P-silent-wrong-device below): any fix that recovers from a *failure* needs a companion check for the *silent-wrong-result* variant of the same bug, because "no exception" is not the same as "correct."

## DEF-173 — Wake word models never provisioned: fresh `pip install` crashes on `heyvox start`

- **Date**: 2026-07-03
- **Category**: integration / config (packaging gap)
- **Severity**: S1 (core feature — wake word → STT → paste — is 100% non-functional for every new PyPI user, on any hardware; `heyvox start` crashes on first foreground run, dies silently as a launchd daemon)
- **Symptom**: A brand-new `pip install heyvox && heyvox setup && heyvox start` never produces a single wake-word detection. Reproduced end-to-end in a fresh venv from PyPI (`heyvox==1.0.0`): `Model(...)` raises `NO_SUCHFILE`/`ValueError` because no model file exists on disk.
- **Root cause**: openwakeword ships NO model files in its wheel — its `resources/models` dir is empty until `download_models()` runs — but `Model()` loads its shared feature extractors (melspectrogram, embedding) + VAD from that dir on every init, plus the requested wake word by bare name. DEF-159 changed the default wake-word *name* to `hey_jarvis_v0.1` "so fresh install boots", but that fixed only the name, not the missing *file*. Compounding: `load_models()` picked `inference_framework="tflite"` whenever no custom `.onnx` path resolved (the default case), and `tflite-runtime` has no wheel on Python 3.12+, so even a name-based load could not succeed. The existing guard `test_default_wake_word_loadable_on_fresh_install` only checked the name against openwakeword's bundled-name set — never that the model *file* was present/loadable — so it stayed green while the real install crashed.
- **Fix**: Bundle the 4 required onnx models (hey_jarvis_v0.1 + melspectrogram + embedding + silero_vad, ~5.2 MB) into the wheel under `heyvox/models/oww/` (added to `pyproject.toml` package-data; added a `.gitignore` exception since `models/` + `*.onnx` were globally ignored — the reason the repo shipped zero models). New `_ensure_oww_models()` in `heyvox/audio/wakeword.py` mirrors the bundled feature models into openwakeword's own dir at load time (offline-first copy; `download_models()` network fallback for non-bundled built-ins or a read-only dir). `load_models()` now forces `inference_framework="onnx"` unconditionally. `heyvox/main.py` wraps the `load_models()` call to print an actionable message + `SystemExit(1)` instead of a raw traceback. `heyvox/setup/wizard.py` gained a "Step 2b: Wake Word Model" provisioning step. Also fixed the sibling packaging gap: `mlx-audio` (a hard runtime import in the TTS daemons) was in no extra → added to `[tts]`.
- **Found by**: Launch-readiness audit — a fresh-venv PyPI install reproduced the crash end-to-end; a follow-up fresh-venv test confirmed the fix (0 models → `load_models()` loads + `predict()` runs).
- **Would have caught earlier**: CI's install-test only ran `heyvox --help`; it never constructed the wake-word `Model()`, so the crash shipped and survived months of development. Added `tests/test_wakeword_provisioning.py` (bundled-files-present, default-resolvable, empty-dir-mirror — all net-free). General pattern: a guard test that checks a *name/config value* instead of the *actual runtime artifact* gives false safety — the fresh-install guard must load the real model, and CI must exercise the real thing (Blocker 4, still pending).

## DEF-174 — `heyvox doctor` crashed with ModuleNotFoundError (module never existed)

- **Date**: 2026-07-03
- **Category**: dead-code / integration
- **Severity**: S2 (the command a stuck user reaches for after a failed start is itself broken — compounds any other first-run failure; no impact on the running daemon)
- **Symptom**: `heyvox doctor` → `ModuleNotFoundError: No module named 'heyvox.doctor'`. The subcommand was registered in `cli.py` (`_cmd_doctor` → `from heyvox.doctor import run_doctor`) and advertised in `--help`, but `heyvox/doctor.py` never existed in the repo or the wheel.
- **Root cause**: An orphaned subcommand — the CLI registration + help text shipped without the backing module. Leaving a registered, documented command that always crashes is worse than not having it.
- **Fix**: Implemented `heyvox/doctor.py::run_doctor()` — a guarded diagnostics report (platform/STT-engine fit, macOS permissions, wake-word model presence + loadability, key optional deps, config location), complementary to `heyvox status`. Every check degrades to a warning line rather than raising. Directly surfaces the DEF-173 fresh-install model-presence class.
- **Found by**: Launch-readiness onboarding audit (reproduced against PyPI 1.0.0), cross-confirmed by project memory (`project_heyvox_doctor_broken`).
- **Would have caught earlier**: A CLI smoke test invoking every registered subcommand's `--help` (and a dry-run) would flag any subcommand whose backing module is missing — belongs in the same CI step as the install-test (Blocker 4).

## DEF-175 — Intel Macs hang 120s per dictation (no Apple-Silicon detection; MLX load never fails fast)

- **Date**: 2026-07-03
- **Category**: platform / timing
- **Severity**: S1 on Intel Macs (every wake-word-triggered dictation blocks exactly `_LOAD_TIMEOUT`=120s then silently discards the recording — the tool looks frozen); no impact on Apple Silicon.
- **Symptom**: On an Intel Mac, a fresh install with the default `stt.local.engine: mlx` starts cleanly (wake word has no MLX dependency) but every dictation blocks 120s before returning `""`. No error dialog, no HUD banner, no early warning at setup.
- **Root cause**: (1) No `platform.machine()` check anywhere — the default `engine: mlx` was used regardless of CPU. mlx-whisper is an optional `[apple-silicon]` extra with no wheel on Intel. (2) `_load_mlx_model()`'s `except ImportError` branch logged and returned without setting any flag or calling `_mlx_loaded.set()`, while `transcribe_audio()` unconditionally did `_mlx_loaded.wait(timeout=_LOAD_TIMEOUT)` — so a missing import blocked the full 120s on every call.
- **Fix**: (1) `heyvox/audio/stt.py`: added a module flag `_mlx_unavailable`, set True in `_load_mlx_model`'s ImportError branch; `transcribe_audio` now checks it up front and polls (instead of a blind 120s wait), bailing the instant the load thread reports unavailability — a 120s hang becomes a near-instant, clearly-logged failure. (2) `heyvox/config.py`: `STTLocalConfig.engine` default is now machine-dependent via `_default_stt_engine()` — `mlx` on arm64, `sherpa` on Intel — so Intel installs never pick MLX. `heyvox doctor` (DEF-174) already surfaces CPU + mlx-whisper status.
- **Found by**: Launch-readiness stability audit (flagged the 120s hang and the total absence of arch detection).
- **Would have caught earlier**: No test exercised the missing-MLX path. Added `tests/test_stt_engine_fallback.py` (fail-fast returns in <5s, not 120s; Intel-defaults-to-sherpa). General pattern: an optional-dependency feature needs an explicit fail-fast plus a test that the *absence* of the dep degrades gracefully rather than hanging.

## DEF-176 — Hush native host opened an unauthenticated TCP port (127.0.0.1:9847) allowing local text+Enter injection into the focused browser tab

- **Date**: 2026-07-03
- **Category**: dead-code / security
- **Severity**: S1 (security) — rated HIGH / pre-launch blocker by the security audit; a working "inject arbitrary text and submit it with Enter into whatever the victim's browser currently has focused" primitive (password fields, web terminals, email compose), reachable by any co-resident local process or a different OS user on a shared Mac. Not remotely reachable (browsers cannot open raw TCP), so the attacker must already run code on the machine; bounded to browser-focused-element injection (no filesystem/subprocess/credential tool exposed).
- **Symptom**: `lsof -iTCP:9847 -sTCP:LISTEN` showed `hush_host.py` (launched by Chrome for the Hush extension) listening on `127.0.0.1:9847`; a bare TCP connect from an unrelated process succeeded with no handshake. Sending `{"action": "type-text", "text": "..."}` / `{"action": "press-enter", "count": N}` relayed straight through Chrome → content script → `document.activeElement`.
- **Root cause**: `_run_servers()` in `heyvox/hush/host/hush_host.py` started a plain `asyncio.start_server` on `127.0.0.1:9847` in addition to the Unix socket, with no authentication and no Origin/Host check (it isn't HTTP). Unlike every other IPC channel in the codebase, a TCP port is not a filesystem object, so the per-user `$TMPDIR` 0700 isolation (and the `os.chmod(..., 0o600)` applied to the Unix socket) cannot protect it — this was the one channel that opted out of the architecture's per-user isolation. It was also dead code: the DEF-105 PID-suffixed Unix socket superseded it, and every client (`injection.py::_hush_send`, `hush/scripts/hush-cli.sh`) connects via `AF_UNIX` only. No consumer of the TCP listener exists anywhere in the repo.
- **Fix**: Removed the TCP fallback entirely from `heyvox/hush/host/hush_host.py` — deleted the `TCP_HOST`/`TCP_PORT` constants, the `asyncio.start_server` block in `_run_servers()`, and the docstring reference. The Unix socket (0600, inside 0700 `$TMPDIR`) remains the sole IPC channel. Chose removal over a shared-secret token because the channel had zero consumers — deleting the door is strictly better than guarding a door nobody uses. Verified no client, doc, JS, or test referenced the port before removal (grep across `*.py`/`*.sh`/`*.js`/`*.md`).
- **Found by**: Launch-readiness security audit (2026-07-02), `.context/release-audit/03-security.md` §3c — confirmed live and listening on the audit machine; called out as the audit's most significant finding.
- **Would have caught earlier**: No test asserted the native host's listener surface, and the dead TCP fallback was never removed when DEF-105 made the Unix socket primary. Added `tests/test_hush_host_security.py` (net-free source guards: no `9847`/`TCP_PORT`/`asyncio.start_server`; Unix socket + 0600 retained; the in-process client stays `AF_UNIX`-only). General pattern: any IPC "fallback" transport needs an actual consumer or it should be deleted, and any listener that cannot inherit the `$TMPDIR` 0700 per-user isolation (a TCP port, by definition) is a defense-in-depth regression that must be flagged at review — see the P-network-listener-bypasses-tmpdir pattern below.

## DEF-177 — Lua string-breakout in Herald orchestrator: workspace label / alert message could inject into `hs -c`

- **Date**: 2026-07-03
- **Category**: string-handling / security
- **Severity**: S2 — an external → local-code-execution chain, but narrow preconditions (Hammerspoon installed and running, hold-queue enabled, and a crafted value reaching the label). Bounded to `hs -c` (Lua) execution as the local user; not remotely reachable.
- **Symptom**: A workspace label containing a backslash immediately before a single quote (e.g. `foo\' .. os.execute('touch /tmp/pwned') .. '`) was not neutralized before being interpolated into the single-quoted Lua string that `_notify_held()` runs via `hs -c`. The crafted `\'` unbalanced the Lua string literal, so trailing tokens ran as Lua rather than being treated as label text.
- **Root cause**: `_notify_held()` escaped the label with `workspace.replace("'", "\\'")` — it escaped the single quote but not the backslash. Since a lone `'` becomes `\'` while a preceding backslash is left untouched, an attacker-supplied `\'` composes into `\\'` in the wrong way: the backslash escapes itself and the quote ends up bare, terminating the string. Escape order must be backslash-first (`\` → `\\`) then quote (`'` → `\'`), the order already used correctly for AppleScript in `injection.py`/`toast.py`. The tainted `workspace` value flows from `get_workspace_label()`, whose precedence includes a Conductor **PR title** pulled from Conductor's SQLite DB — an externally-authored string on any public repo the user opens a workspace against. The sibling `_show_alert()` (herald/orchestrator.py) did **zero** escaping of its `message` arg — safe only because today's callers pass int-formatted strings (`f"{len(remaining)} more pending"`), but the same injection class the instant any caller passes user text.
- **Fix**: Added a module-level `_lua_str_escape()` helper in `heyvox/herald/orchestrator.py` — escapes backslash first, then single quote, then neutralizes raw CR/LF (which would otherwise terminate a single-quoted Lua string). Routed both `_notify_held()` (workspace label) and `_show_alert()` (message) through it. `_show_alert` is escaped pre-emptively despite its currently-safe callers, closing the latent path rather than relying on caller discipline.
- **Found by**: Launch-readiness security audit (2026-07-02), `.context/release-audit/03-security.md` §1 — reproduced the `\'` breakout against a local Lua interpreter.
- **Would have caught earlier**: No test exercised the Lua-escaping of either call site (the AppleScript escaping in `injection.py` was tested; the Lua path was not). Added `tests/test_herald_lua_injection.py` — an adversarial round-trip that decodes the escaped output as a Lua single-quoted string and fails on any bare quote / dangling backslash (proving no breakout), plus behavioral coverage that both `_notify_held` and `_show_alert` embed only the escaped form in the `hs -c` argv. General pattern: whenever a value is interpolated into a *different* language's string literal (Lua, AppleScript, SQL, shell), the escaping must match that target language's actual rules and be applied in the composition-safe order (escape the escape character first) — and every such site needs a breakout test, not just the first one written.

## DEF-178 — MCP shared-HTTP server has no per-user auth; loopback bind was the only thing protecting the TTS-control tools (documented + regression-guarded)

- **Date**: 2026-07-03
- **Category**: security / config
- **Severity**: S3 — bounded blast radius (TTS control only: `voice_speak`/`voice_queue`/`voice_config`; no filesystem/subprocess/credential tool exposed), opt-in transport, and the default install has no port at all. The latent regression it guards against (a `0.0.0.0` bind) would be materially worse.
- **Symptom**: The opt-in `python -m heyvox.mcp.server --transport streamable-http` server (port 8014) has no application-level authentication. On a multi-user Mac, another local account — or any already-running local process — can call the TTS-control tools. FastMCP's auto Origin/DNS-rebinding validation (confirmed live: forged Origin → 403) closes the malicious-webpage vector but does nothing for a non-browser local client with no Origin header.
- **Root cause**: `FastMCP("heyvox", stateless_http=True)` never passes `auth=AuthSettings(...)`, so `RequireAuthMiddleware` is never engaged. The design relies on the 127.0.0.1 bind for isolation, which stops network and browser attackers but not same-machine other-user/other-process access. The bind being loopback was load-bearing but unguarded — nothing prevented a future `--host 0.0.0.0` that would expose the unauthenticated tools to the whole LAN *and* silently disable FastMCP's Origin protection (which only activates for loopback binds).
- **Fix**: Chose documented risk-acceptance + a bind guard over building token auth. Rationale: HeyVox is a single-user personal product; the default (and wizard-registered) transport is `stdio`, which opens no port; the blast radius is TTS-only; and token auth would need custom middleware in FastMCP's Starlette app, a header on the client registration, and a migration for the already-registered `vox` client — invasive and unverifiable net-free, disproportionate to a TTS-only cross-account nuisance. Instead: (1) a `Security` block in `heyvox/mcp/server.py`'s module docstring disclosing the no-auth assumption and the stdio/firewall mitigations for shared Macs; (2) `_is_loopback_host()` + a loud stderr warning when `--host` is overridden away from loopback, guarding the more-severe `0.0.0.0` regression the audit flagged. **This is a risk-acceptance decision, not a full mitigation — flagged for Franz to override with per-install token auth if shared-Mac support becomes a requirement.**
- **Found by**: Launch-readiness security audit (2026-07-02), `.context/release-audit/03-security.md` §3b (verified live: bind is `127.0.0.1`, forged Origin → 403, no-Origin curl → 200).
- **Would have caught earlier**: Nothing would surface this as a runtime symptom (it's a design gap, not a crash). Added `tests/test_mcp_server_security.py` (net-free): `_is_loopback_host` accepts only loopback forms and rejects routable addresses; the `--host` default stays `127.0.0.1`; the non-loopback warning branch and the no-auth disclosure stay in source. General pattern: when a security property is enforced *implicitly* by a default (here, "loopback bind ⇒ Origin protection + no LAN exposure"), add a test that pins the default and a runtime guard that warns when it is overridden — implicit protections silently disappear under a one-line config change.

## DEF-179 — Model downloads unpinned (floating HEAD), unvalidated QWEN_TTS_MODEL override, and the setup wizard downloaded a Kokoro repo nothing loads

- **Date**: 2026-07-03
- **Category**: config / integration (supply chain)
- **Severity**: S3 — no active exploit, but a real supply-chain exposure: an upstream repo update (or a compromised HF maintainer account) silently changes the weights every fresh install pulls, and the wizard both wasted a ~300 MB download and vouched for a repo the runtime never uses.
- **Symptom**: Every runtime model load resolved a floating ref: `mlx_audio.load_model("mlx-community/Kokoro-82M-bf16")` (Kokoro daemon), `mlx_audio.load_model(os.environ.get("QWEN_TTS_MODEL", "...bf16"))` (Qwen daemon — env override used verbatim, no validation), and `mlx_whisper.transcribe(path_or_hf_repo="mlx-community/whisper-small-mlx")` (STT) all pull current `main` with no revision pin or hash check. Separately, `heyvox setup` (`wizard.py`) ran `snapshot_download("hexgrad/Kokoro-82M")` — a PyTorch repo the runtime never loads (the daemon loads `mlx-community/Kokoro-82M-bf16` via mlx-audio) — so the wizard downloaded the wrong ~300 MB and its cache-presence check pointed at a directory nothing reads.
- **Root cause**: The mlx loaders take a pin differently and neither pins by default: `mlx_audio.load_model` forwards a `revision=` kwarg to `get_model_path` but callers never passed one; `mlx_whisper` exposes no `revision` at all (but reads a local snapshot dir directly). No central registry of tested SHAs existed. `QWEN_TTS_MODEL` flowed from env into a model download with zero validation. The wizard's repo string was a copy-paste from an earlier PyTorch-Kokoro design never updated to the mlx-audio repo actually shipped.
- **Fix**: Added `heyvox/model_pins.py` — canonical `MODEL_REVISIONS` registry (SHAs verified live against the HF API), `revision_for()` (SHA, for mlx_audio's `revision=` kwarg), `resolve_pinned()` (local snapshot dir at the pinned revision, for mlx_whisper; **never raises — falls back to the bare repo id so pinning can't load worse than the unpinned baseline**), and `validate_model_override()` (trusted-org / local-dir allowlist). Wired it: wizard downloads the loaded repo (`mlx-community/Kokoro-82M-bf16`) pinned; `stt.py` resolves the pinned local dir once in `_load_mlx_model` and passes it to `transcribe` (repo string kept for glossary/logging; re-resolves on reconfigure); the two TTS daemons — which run in a *separate interpreter that cannot import heyvox* — inline their one SHA + a `revision=` load and (Qwen) validate `QWEN_TTS_MODEL` against the trusted org / a local dir. Verified all three pin mechanisms load end-to-end from the pinned revision (mlx_audio `revision=` kwarg; mlx_whisper local snapshot dir) before committing — the pinned SHAs are the currently-cached HEADs, so existing installs see no behavior change.
- **Found by**: Launch-readiness supply-chain audit (`.context/release-audit/05-supply-chain.md` + security §7).
- **Would have caught earlier**: No test asserted model-load pinning or the wizard/runtime repo agreement. Added `tests/test_model_pins.py` (SHA-shape, pass-through for unknown models, graceful fallback on resolution error, override validation — all net-free with `snapshot_download` monkeypatched) and `tests/test_daemon_model_pins.py` (Qwen override validation + a **drift guard** asserting each daemon's inlined SHA equals the canonical registry, since the daemon isolation forces the SHA to live in two places). Residual risk logged: the daemons' *integrated* pinned load is not runtime-verified in this session (separate interpreter + GPU + multi-GB); the `revision=`/local-dir mechanisms were each verified in isolation and the fallback guarantees no regression below baseline. General pattern: pin every external artifact to an immutable digest with a central registry + a test that the digest is a digest (not a moving ref), and when isolation forces a value to be duplicated, add a drift guard rather than trusting a "keep in sync" comment.

## DEF-180 — CI had no secret-scanning or dependency-CVE gate

- **Date**: 2026-07-03
- **Category**: config (CI process gap)
- **Severity**: S3 — no active vulnerability; a missing recurring gate. Its absence is exactly what let one-time audits substitute for continuous scanning.
- **Symptom**: All three workflows (`ci.yml`, `install-test.yml`, `publish.yml`) ran functional tests + ruff + actionlint only — no `gitleaks`/`trufflehog` secret scan and no `pip-audit`/`safety` dependency-CVE check. A secret committed in a future push, or a newly-disclosed CVE in a transitive dependency, would go unflagged.
- **Root cause**: Never added; the audits doing secret/dep scanning were manual, point-in-time passes, not a repeatable CI gate.
- **Fix**: Added a `security` job to `ci.yml` (ubuntu-latest): (1) **gitleaks** secret scan — version-pinned to 8.30.1 **and sha256-checksum-verified** (supply-chain hygiene for the scanner binary itself), run as `gitleaks dir .`; **blocking**, since a leaked credential is always author-actionable. (2) **pip-audit** dependency-CVE audit — **non-blocking** (`continue-on-error`), because heyvox transitives (starlette via FastMCP, urllib3, requests) periodically carry CVEs that need an upstream release, and a hard gate would red CI on issues the maintainer can't immediately fix; it runs on every push for visibility and can be promoted to blocking once the tree is clean. Added `.gitleaks.toml` extending the default ruleset with one allowlist entry: the Hush Chrome-extension `manifest.json` embedded **public** key (base64 RSA SubjectPublicKeyInfo), which gitleaks' `generic-api-key` rule flags on entropy — a genuine false positive (the key is public by design, pins the extension ID, ships in every browser).
- **Found by**: Launch-readiness audit §4 (secrets) + §7 (supply chain), both recommending these as the ongoing repeatable gate.
- **Would have caught earlier**: This *is* the caught-earlier mechanism. Notably, standing up the gitleaks gate immediately surfaced one finding the earlier keyword/pickaxe history scan had not (the extension public key) — validating that the broader-ruleset scanner earns its place even though that specific hit was a false positive. General pattern: replace one-time manual security passes with a pinned, checksum-verified CI gate; block on author-actionable classes (secrets), report-only on externally-gated classes (transitive CVEs) until the baseline is clean.

## DEF-181 — Public privacy page made two now-false categorical claims ("no telemetry"; "transcriptions never leave your computer")

- **Date**: 2026-07-03
- **Category**: config (documentation) / ux (trust)
- **Severity**: S2 — no code defect (both features are correctly opt-in, default-off, fail-closed), but publicly false privacy claims on a privacy-first product are a real trust/reputation and potential legal risk.
- **Symptom**: `docs/privacy.html` (live on the public site) stated categorically: "nothing you do... is sent anywhere, ever"; "speech transcriptions... never leave your computer"; "There are no... servers operated by us"; "no telemetry"; "no feature-usage tracking"; "We do not share any data with third parties." All are false in specific opt-in configurations: (1) opt-in telemetry (shipped ~May 2026) POSTs to `heyvox.dev/telemetry/v1/events`; (2) the opt-in `vocab_learner` (`heyvox learn-vocab`) sends dictation transcript *text* to the Anthropic API.
- **Root cause**: The privacy page predated both opt-in network features and was never updated when they landed — the doc and the code diverged silently (no test or checklist ties privacy-page claims to actual network call sites).
- **Fix**: Rewrote the affected sections of `docs/privacy.html` (summary, data-we-collect, audio/speech, analytics/telemetry, third-parties, GDPR, intro tagline, "last updated" date) to disclose both opt-in paths accurately while preserving the true and important claims: audio never leaves under any configuration; both features are off by default and fail closed; telemetry payload is version + hashed hostname + 5 numeric counters with no transcript/log/path/config content. Framed as opt-in exceptions, not blanket denials.
- **Found by**: Launch-readiness security/privacy audit (`.context/release-audit/03-security.md` §6, blocker 10 / SHOULD 12).
- **Would have caught earlier**: Nothing tied the doc's claims to code behavior. Added `tests/test_telemetry_payload.py` — a net-free guard that plants sensitive transcript-like text into the log the counter reader scans and asserts it NEVER reaches the built payload (only integer counts + an allowlisted set of system fields do; hostname is hashed, not raw), plus pins the tracked-tag set so a new counter is a conscious, reviewed change. This backs the telemetry half of the privacy claim with an executable check. The vocab-learner disclosure remains doc-only (its network path is inherent to the feature). General pattern: a categorical privacy claim in user-facing docs should be backed by a test asserting the actual payload shape, and any new outbound network path must trigger a privacy-page review (candidate for a checklist/CI reminder keyed on new `urlopen`/`requests`/`httpx` call sites).

## DEF-182 — Stale duplicate Chrome extension at repo root (diverged from the canonical copy)

- **Date**: 2026-07-03
- **Category**: dead-code
- **Severity**: S3 — no runtime impact, but a diverged duplicate misleads contributors and security reviewers and risks someone side-loading the wrong (out-of-date) copy.
- **Symptom**: `chrome-extension/` at the repo root tracked 4 files (`background.js`, `content.js`, `manifest.json`, `README.md`) duplicating the canonical extension at `heyvox/hush/extension/`. The two had diverged — the root `manifest.json` differs, and the root copy lacks `icons/`, `popup.html`, `popup.js` that the canonical one ships.
- **Root cause**: An earlier standalone extension location, superseded when Hush was merged into the `heyvox` package (`heyvox/hush/extension/`), but never removed after the monorepo consolidation.
- **Fix**: `git rm -r chrome-extension/`. Verified before removal that the only reference to it was its own README (no code, setup script, doc, or `pyproject` pointed at the root copy) and that `heyvox/hush/extension/` is the canonical, more-complete copy the native-messaging host + install flow use.
- **Found by**: Launch-readiness onboarding audit (#10 / SHOULD 15).
- **Would have caught earlier**: No check flags a tracked directory that nothing references. General pattern (low priority): a duplicate-asset lint (e.g. two `manifest.json` extension roots) or a "no orphaned top-level dirs" convention would surface this; not worth automating for a one-off, but worth a glance during monorepo consolidations — merging repos is exactly when a superseded copy gets left behind.

## DEF-183 — Missing community-health files (SECURITY/CONTRIBUTING/CHANGELOG) and a dead `Changelog` URL

- **Date**: 2026-07-03
- **Category**: config (packaging / documentation)
- **Severity**: S3 — no runtime impact; a public-repo hygiene gap (no security-reporting path, no contributor guide, no changelog) plus a packaging metadata link that 404s in spirit.
- **Symptom**: A public, already-launched repo had no `SECURITY.md` (nowhere for a researcher to report privately — worse than usual right after a security hardening pass), no `CONTRIBUTING.md`, and no `CHANGELOG.md`. `pyproject.toml`'s `[project.urls] Changelog` pointed at `.../releases`, which has zero GitHub Releases despite three tags — an effectively dead page shown on the PyPI project sidebar.
- **Root cause**: Never created; the project shipped v1.0.0 without standard community-health files, and the Changelog URL was a placeholder pointing at an unused releases page.
- **Fix**: Added `SECURITY.md` (private reporting via GitHub advisories / email, supported-versions, and the single-user-Mac threat model consistent with the Phase-2 findings), `CONTRIBUTING.md` (dev setup, tests/lint, conventions incl. the no-app-hardcoding + local-audio rules; keeps the internal defect-log as a maintainer-only note so it doesn't bind external contributors or depend on `.planning/` being public), and `CHANGELOG.md` (Keep-a-Changelog; `[Unreleased]` captures the launch-hardening work, `[1.0.0]` marks the initial release). Repointed the `pyproject` `Changelog` URL at `CHANGELOG.md` (a page that exists) rather than the empty releases list.
- **Found by**: Launch-readiness onboarding audit (#7 / SHOULD 2).
- **Would have caught earlier**: GitHub's own "community profile" checklist flags missing SECURITY/CONTRIBUTING; a release checklist would flag a Changelog URL with no releases behind it. Not worth automating, but both are standard pre-launch gates. Note deliberately deferred: the README "(coming soon)" on the PyPI install stays until Blocker 6 cuts a working release — claiming `pip install heyvox` works today would be a false statement (the shipped 1.0.0 is unrunnable), the same class of issue as DEF-181.

## DEF-184 — README documented only 10 of 21 CLI commands; no troubleshooting or uninstall guidance

- **Date**: 2026-07-03
- **Category**: config (documentation)
- **Severity**: S3 — onboarding friction; a stuck user has no in-README path to self-serve (diagnose, uninstall, or discover half the CLI).
- **Symptom**: `README.md` documented 10 of the 21 `heyvox` subcommands. Missing: `doctor`, `commands`, `quiet`, `verbose`, `debug`, `log-health`, `calibrate`, `bugreport`, `telemetry`, `register`, `learn-vocab`. No Troubleshooting/FAQ section and no Uninstall instructions despite `scripts/uninstall.sh` existing.
- **Root cause**: README grew feature-first; new CLI subcommands were added without a corresponding docs pass, and support-oriented sections (troubleshoot/uninstall) were never written.
- **Fix**: Documented every subcommand grouped by purpose (core, TTS, diagnostics & maintenance, MCP registration, telemetry, vocabulary learning), with the two opt-in network features (`telemetry`, `learn-vocab`) explicitly flagged off-by-default and linked to the privacy policy. Added a Troubleshooting section (anchored on `heyvox doctor`) and an Uninstall section (`scripts/uninstall.sh`, incl. `--keep-config`). Verified 21/21 commands are now referenced and that all in-README anchor links resolve. Command names/flags were read from `cli.py` (choices verified) rather than assumed — e.g. `telemetry` actions `status|enable|disable|preview|reset-id`.
- **Found by**: Launch-readiness onboarding audit (#8 / SHOULD 3).
- **Would have caught earlier**: A doc-coverage check (the exact one used to verify this fix: parse `add_parser` names from `cli.py`, assert each is referenced in the README) could run in CI as a cheap guard. Left as a manual check for now — parser introspection is brittle to argparse refactors — but noted as a candidate. General pattern: user-facing command surfaces drift from their docs unless a coverage check ties them together.

## DEF-185 — Internal business-strategy docs (product-concept.md, .planning/PROJECT.md, .planning/research/) committed to the public repo

- **Date**: 2026-07-03
- **Category**: config (repo hygiene) / ux (competitive exposure)
- **Severity**: S2 — no secrets/credentials (so no rotation needed), but business-model, pricing, revenue-target, and competitor-pricing content on a public repo is a real competitive/reputational exposure.
- **Symptom**: `product-concept.md` (935 lines, "Draft/Internal") and `.planning/PROJECT.md` were public on `origin/main` (511 commits) and contained the business model (OSS core + Pro tier, $12/mo / $99/yr), the revenue target ($72K ARR at month 24), and competitor pricing (Wispr Flow $30M funded, SuperWhisper $849). `.planning/research/` (competitive feature analysis) was likewise public. All were listed in `.gitignore` ("not for public release") but had been committed before the ignore rule took effect, so ignore had no effect.
- **Root cause**: Files committed before being added to `.gitignore`; git does not retroactively untrack. Compounded by the `.planning/` defect-log workflow legitimately committing into the same directory, which masked that the *strategy* subset was also being tracked and pushed.
- **Fix**: Two parts. (A, this commit) `git rm --cached` the three paths (untracked from the tip, local copies preserved) and rewrote `.gitignore` to reflect the decided intent — `CLAUDE.md` and the rest of `.planning/` (incl. DEFECT-LOG.md) are intentionally PUBLIC (so `.planning/` and `CLAUDE.md` removed from ignore, un-masking the defect-log workflow), while `product-concept.md` + `.planning/PROJECT.md` + `.planning/research/` stay ignored to prevent re-adding. (B, separate, out-of-band) purge the three paths from ALL git history via `git filter-repo --invert-paths` on a fresh mirror clone + force-push all refs, then re-clone every working copy. Scope + keep/purge decisions made by Franz. The filter-repo command was validated on an isolated mirror before execution (3 targets gone from all refs, 525→519 commits, everything else intact, fsck clean).
- **Found by**: Launch-readiness onboarding/security audit (blocker 9). The PROJECT.md portion was found this session by scanning `.planning/` for pricing/revenue/competitor content after Franz initially leaned toward keeping `.planning/` public — the scan surfaced that PROJECT.md carries the same class of content as product-concept.md.
- **Would have caught earlier**: A pre-publish check that no `.gitignore`-listed path is also tracked (`git ls-files $(git check-ignore *)` style) would have caught the committed-then-ignored files before they ever went public. Deeper lesson: content already public via 511 commits + forks/clones/caches cannot be fully unrung by a history rewrite — the rewrite reduces future exposure but is not a substitute for treating anything truly sensitive (real secrets) as compromised. Here the content is competitive, not credentials, so rewrite + accept residual is the proportionate response.

## DEF-186 — main CI went red after the launch merge: huggingface_hub + sounddevice were `[tts]`-only but used by core code + tests

- **Date**: 2026-07-03
- **Category**: config (packaging) / regression
- **Severity**: S2 — public `main` CI red immediately after the launch-hardening merge; breaks the green-main invariant.
- **Symptom**: The "Lint & Test" CI job failed on `main` right after all 36 launch commits landed: 7 failures / 1047 passed — `tests/test_model_pins.py` ×3 (`ModuleNotFoundError: No module named 'huggingface_hub'`) and `tests/test_cues.py` ×4 (`ModuleNotFoundError: No module named 'sounddevice'`). Install-Test + Security jobs passed.
- **Root cause**: `huggingface_hub` and `sounddevice` were declared only in the `[tts]` optional-dependencies extra, but both are used by CORE code paths — `huggingface_hub` downloads every model (wake word, STT, and TTS) and backs DEF-179 revision pinning; `sounddevice` is the primary cue-playback path (afplay is a fallback). CI installs `.[dev,chrome]` (not `[tts]`), so the tests importing them couldn't import them. Classic local-green/CI-red environment coupling: the local dev env had both (from other projects / a full install), masking the gap until CI ran on the merged main.
- **Fix**: Moved `sounddevice>=0.4.0` and `huggingface_hub>=0.20.0` from the `[tts]` extra to core `[project.dependencies]` (one `pyproject.toml` change; no test files touched). CI's `.[dev,chrome]` install now pulls them, so the tests import cleanly. `[tts]` retains only `kokoro` + `mlx-audio`.
- **Found by**: Watching CI after the Phase-3 force-push — the merge + force-push triggered a run on rewritten `main` that failed; inspected `gh run view --log-failed`.
- **Would have caught earlier**: Running the suite in a clean `.[dev,chrome]`-only venv before pushing (exactly the coupling flagged by prior defect-log patterns — local pytest sees a fuller env than CI). General pattern: a dependency imported by any core code path (or by a test that isn't guarded with `importorskip`) must be a core dependency, never buried in a feature extra; and "green locally" is not "green in CI" whenever the local venv is a superset of the CI install.

## DEF-187 — Shipped PyPI 1.0.0 was stale + unrunnable; no tag↔version guard; README said PyPI "(coming soon)"

- **Date**: 2026-07-03
- **Category**: config (release) / regression
- **Severity**: S1 pre-fix (the published package `pip install heyvox` crashed on first run — the headline launch blocker) → resolved by cutting a runnable release.
- **Symptom**: PyPI had `heyvox 1.0.0`, which was ~16 commits stale and crashed on `heyvox start` (the wake-word-model provisioning bug + others, all fixed across Phases 1-3 but never released). The README's install section said "From PyPI (coming soon)" (false — it WAS on PyPI, just broken) and listed a non-existent `[full]` extra. Nothing enforced that a release tag matched `pyproject` version.
- **Root cause**: No release was cut after the Phase 1-3 fixes landed; the working code lived only on `main`, not in any published artifact.
- **Fix**: Cut **v1.1.0** (minor bump — the hardening added `doctor`/`telemetry`/`learn-vocab` surfacing, security fixes, and made the install actually work; can't reuse the burned 1.0.0 on PyPI anyway). Bumped `pyproject` version (the sole source — `__version__` derives from `importlib.metadata`), promoted CHANGELOG `[Unreleased]`→`[1.1.0]`, flipped the README PyPI section to real instructions (dropped the non-existent `[full]` extra), and added a **tag↔pyproject version guard** step to `publish.yml` (fails the publish if `v<tag>` ≠ pyproject version — the CI check the audit requested). Tag `v1.1.0` push triggers `publish.yml` → build + Trusted-Publisher (OIDC) upload to PyPI.
- **Found by**: Launch-readiness onboarding audit (blocker 1 + blocker 6 / headline).
- **Would have caught earlier**: A release checklist (version bump + changelog + tag == version) and the now-added `publish.yml` guard. Also a "does the published wheel actually run" smoke test — the `install-test.yml` workflow now loads the model (DEF-173 guard), which would have caught the original unrunnable 1.0.0 before it shipped. General pattern: a green `main` isn't a shipped fix — the release step (and a guard that the artifact runs) is part of "done."

## DEF-188 — First command after every daemon (re)start paid a full cold-load; no start-preload

- **Date**: 2026-07-05
- **Category**: timing / ux
- **Severity**: S2 (perceived latency: first command after each restart ~6.5s vs warm ~1s; no data loss, no crash)
- **Symptom**: Franz repeatedly reported "transcription slow again." Warm STT is fast and stable (p50 1.08s, RTF 9–15× realtime, steady across 6 days), but the cold-load share spiked to 20% on 2026-07-04 (normal ~2%). The concretely-experienced case: the test trigger "Okay, jetzt möchte ich testen, wie schnell das Transkripieren geht" (23:41) ran cold (warm=False, 2.83s STT / 4.32s audio = 1.5× realtime) right after a SIGTERM restart at 23:34.
- **Root cause**: `preload_model()` (`heyvox/audio/stt.py`) was only called at recording START (`recording.py`), never at daemon start. That hides the cold-load behind speaking time — but only if the model stays warm between recordings. DEF-164 (idle-timer) + `unload_secs=86400` (2026-07-04) keep it warm between recordings, but a process kill (SIGTERM — the daemon was restarted 8× in one day) discards the whole model, and the lazy load on the next recording is un-hidden again for short commands (load time > recording length).
- **Fix**: Start-preload in `heyvox/main.py` — after `init_local_stt(...)`, gated on `engine == "mlx"`, a non-blocking `preload_model()` + log "STT: warming MLX model at startup". The model loads in the background at daemon start (~10s observed) so the first command after any (re)start is warm. `_load_mlx_model` is double-guarded (`_mlx_loaded.is_set()` before and after the lock) against a race with the first wake-word preload. Verified: after `launchctl kickstart -k`, "warming MLX model at startup" → "MLX model loaded in 10.0s" appears with NO trigger.
- **Found by**: Franz's repeated "slow" reports → historical RTF analysis from `heyvox-stt-debug.log` (warm consistently fast; cold-share spike correlated with a cluster of SIGTERM restarts).
- **Would have caught earlier**: A "first-command-after-restart" latency test would have shown that the lazy-preload design point ("hides behind the recording") does not hold for short commands under frequent restarts. **Open follow-up**: WHY 8 SIGTERM restarts/day? (source unconfirmed — likely parallel Conductor sessions restarting HeyVox after code changes). The start-preload treats the symptom robustly regardless of restart source; the source remains separately worth pinning down.

## DEF-189 — DEF-104 hotplug self-heal only considered mic_priority devices; an actively-used non-priority mic never recovered

- **Date**: 2026-07-05
- **Category**: integration / state-pollution
- **Severity**: S2 (mic silently stuck on the built-in fallback; recoverable only by manual restart, not by the daemon itself)
- **Symptom**: Franz's G435 (USB Lightspeed dongle) "wasn't being accepted." macOS Sound settings showed the G435 with a live input meter and a fresh `pyaudio` process saw it as the default input, but the running daemon stayed on MacBook Pro Microphone and every manual HUD pin failed with `Manual pin failed: 'G435 ...' still has no input`. The daemon even logged misleading `BT HFP probe / likely BT A2DP` lines **for a USB device**.
- **Root cause**: Two combined. (1) G435 was not in `mic_priority` (trimmed 2026-06-09 for an unrelated BT-drama). (2) DEF-104 stale PortAudio cache: the G435 was a post-start USB hotplug, so PortAudio's process-global CoreAudio list still held its output-only (A2DP-looking) enumeration — the daemon never saw the input endpoint. The DEF-104 self-heal (`detect_missed_hotplug`) *only* treated `mic_priority` entries as candidates, so a live-but-uncached device absent from that list produced no restart. The self-heal that was built to fix exactly this (`_maybe_restart_for_hotplug`) could not fire because the affected mic wasn't a priority device.
- **Fix**: `detect_missed_hotplug` now also accepts the CoreAudio **default input** as a fallback candidate (`heyvox/audio/device_handle.py`): when no priority device matches, an actively-used default input showing the same live-but-uncached signature (and not already the mic in use) is flagged too — macOS makes a freshly-plugged USB headset the default input, a strong "user wants this mic" signal the priority list alone missed. New `get_default_input_device_name()` reads it from the live HAL via ctypes (`heyvox/audio/mic.py`); `_maybe_restart_for_hotplug` passes it through. The DEF-147 Bluetooth exclusion and the restart-loop guard still wrap the new candidate (both operate on the returned `missed` name), so BT default inputs are still never restart-triggers and a device that stays invisible after one restart won't spin the daemon. The early-return now gates on CoreAudio availability, not on a non-empty priority list, so the default-input path works with an empty `mic_priority`. Immediate incident also resolved by adding G435 to `mic_priority` + `launchctl kickstart`. 7 new `test_def104_default_input_*` guards; existing 4-arg `detect_missed_hotplug` calls stay valid (new param defaults to None).
- **Found by**: Live diagnosis of Franz's "mic not accepted" report — fresh-process vs daemon enumeration mismatch + `mic_priority` inspection.
- **Would have caught earlier**: The DEF-104 design assumed the wanted mic is always in `mic_priority`; a test for "actively-used default input NOT in the priority list" would have exposed the gap. General pattern: self-heal candidate sets derived only from static config miss devices the user selects at the OS layer at runtime — the live CoreAudio default input is the authoritative "what the user wants now" signal and should be a first-class candidate, not config-gated.

## DEF-190 — Stop-wake "Hey Vox" appended without a ≥0.5s pause was blocked by the pre-silence gate

- **Date**: 2026-07-05
- **Category**: ux / timing (wake word)
- **Severity**: S2 (start-wake works, stop-wake needs 2–5 tries; recoverable via a deliberate pause or Escape/fn, no data loss)
- **Symptom**: Franz reported the **start** "Hey Vox" works reliably but the **stop** "Hey Vox" needs 2–5 repeats. Initially misdiagnosed (see below). The failing stop attempts left **no score log at all** — which looked like a dead/intermittent mic, but the real tell was that when the stop *did* register, the score was a flat 1.000, never a near-miss.
- **Root cause**: The DEF-096 pre-silence gate + `_VAD_SILENT_GRACE = 0.5` (`heyvox/main.py`). A frame only counts as "silent" if there was no non-silent activity in the prior 0.5s. When a stop "Hey Vox" is appended to an utterance with only a short (<0.5s) word-boundary pause, no silent frame is registered (`last_silent=never`), so the gate blocks the stop unless the score clears the `_ULTRA_CONFIDENCE_FAST_STOP = 0.999` bypass. A casually-appended stop word scores ~0.85–0.99 → blocked → the user repeats until one lands ≥0.999 or with a real pause. Confirmed by log line `[NEAR_MISS_WINDOW_BLOCKED] score=1.000 ... last_silent=never` → `[STOP_PATH] path=ultra`. **NOT** the mic or level: a live capture showed the G535's ambient VAD median = 6 (p90 7), far below the 500 `silence_threshold` — the gap was the grace duration, not the level.
- **Fix**: `_VAD_SILENT_GRACE` 0.5 → 0.3 so a normal word-boundary pause registers as silence and the DEF-096-B discount / window path can fire. Kept ≪ the ~1s feature window so dead-mic silence bursts still register. **Trade-off (user chose this over behaviour change / Escape-to-stop)**: slightly higher mid-sentence false-stop risk — measurable via `STOP_PATH path=ultra` / false `Stopping recording` lines. The 0.999 ultra-bypass was deliberately NOT lowered (documented mid-sentence phoneme flares hit 0.982/0.997).
- **Found by**: Franz's precision "start works, stop is worse" cut through several wrong turns.
- **Would have caught earlier**: I burned real time on wrong theories first — BT-A2DP zombie (the G535 has no BT mode), the "Wireless" name triggering BT classification (BT is detected by CoreAudio transport type, not name), and volume-ducking distortion — before Franz's start-vs-stop distinction pointed at the stop-only pre-silence gate. Process gap: for a "wake word unreliable" report, **first ask start-vs-stop** — they run through entirely different gates (start = score-only; stop = score + pre-silence + min-recording + cooldown). A one-line diagnostic that classifies each miss as blocked-by-gate vs not-heard-at-all (score log present?) would have pointed here in one step. Also: `_VAD_SILENT_GRACE`, `silence_threshold`, and the two stop thresholds are all per-mic-sensitive tunables with no single "why did this stop fail" trace — the DEF-149/151 STOP_MISSED tags help but don't say *which* gate blocked.

## DEF-191 — Garbled filter's catastrophic-ratio guard discarded coherent dictation just because STT was slow

- **Date**: 2026-07-06
- **Category**: error-handling / regression
- **Severity**: S2 (a full, coherent utterance was silently lost + 21.2s wasted on re-verify; raw audio is preserved on disk but the user loses the turn)
- **Symptom**: Franz's coherent dictation "Lies bitte meine Konversation die letzten mit Andrew und hilf mir eine gute Frage" was discarded. `FILTER (garbled, stt=11.4s)` at 17.4s audio → 11.4s STT (ratio 0.66). Re-verify with large-v3 *also* discarded after 21.2s → utterance lost. Reported together with slow STT + audible crackling during a high-load window (4 parallel Conductor sessions, 15-min load 6.77).
- **Root cause**: The DEF-083/093 catastrophic-ratio guard (`heyvox/text_processing.py`) discards when `stt_secs/audio_secs > 0.6` on ≥5s audio with **no content check** — a time-only proxy for "temperature fallback thrashed → hallucination". Slow-but-COHERENT STT (system load, quiet G535 mic at −37 dBFS, GPU contention from parallel Kokoro/STT) trips it and discards real text. Exactly the open bug tracked in memory `bug_garbled_ratio_guard_kills_slow_stt`; the guard's own comment already documented a clean-text false-discard on 2026-04-27 (ratio 0.35).
- **Fix**: The ratio guard now requires a **degenerate shape** in addition to the slow ratio: it only discards if the text is NOT coherent — `< 5 words OR unique-word-ratio < 0.5`. A sentence that cleared every text-level check (repetition, bigram, run-length, intra-token, tail-window) AND shows high word diversity is real speech that was merely slow. The 0.6 threshold is deliberately unchanged (memory warned re-tuning it just moves the false-discard boundary). Verified: Franz's text → kept; `doc doc…` loop / grey-zone (uniq 0.44) / short output → still discarded. 148 guard tests green + new `test_def191_ratio_guard_keeps_coherent_slow_stt`.
- **Found by**: Franz's "recording dropped + slow transcribe + crackling" report → `FILTER (garbled, stt=11.4s)` on coherent text in `/tmp/heyvox.log`.
- **Would have caught earlier**: The guard's own comment already flagged clean-text false-discards as a recurring shape; a content-aware condition should have been added when DEF-093 raised the threshold 0.3→0.6 (that only moved the boundary, didn't remove the false-positive class). **Still open / separate**: the *cause* of the slowness (system load + quiet mic + GPU contention across parallel sessions) — this fix stops coherent text from being lost to it but does not make STT faster. The re-verify path also burned 21.2s on already-coherent text; re-verify should be skipped when the primary output already passed the (now content-aware) checks.

## DEF-192 — Conductor paste stuck on the slow osascript Cmd+V path; a stale "AX breaks React" skip had disabled the sub-millisecond AX fast-path

- **Date**: 2026-07-08
- **Category**: dead-code / timing / ux
- **Severity**: S3 (paste worked correctly throughout — this is latency, not a failure; the ~1.0s osascript paste was the single biggest post-STT cost after transcription itself)
- **Symptom**: Franz: the paste/overhead block after STT "muss viel schneller gehen." Per-step timing showed the osascript `app_fast_paste` (focus-shortcut + Cmd+V + Enter in one subprocess) cost ~1036ms — about a third of the whole voice→text→inject latency — even though the actual value insert is sub-millisecond.
- **Root cause**: Two compounding issues. (1) `injection.py::_ax_inject_text` skipped the AX direct-insert fast-path for workspace-managed Electron (Conductor) via a `conductor_workspace_id` guard whose comment claimed "AXValue set returns success but React state stays empty → Enter submits blank." True on an older Conductor, but **fixed upstream** by newer Electron/Chromium (`SetValue` w/ `kDispatchInputAndChangeEvent`, Electron PR #38102) — the skip had become a stale dead-path (re-tested live: a marker set via AXValue was correctly submitted on Enter). (2) The Conductor paste doesn't even go through `_ax_inject_text` — it routes via `app_fast_paste` (Tier 2, because Conductor capture yields `role_path_hops=0` so Tier 1 is skipped), which never called the AX path at all. And the capture's `leaf_role` comes from the **mouse position** at record-start, so dictating with the mouse off the field yielded `AXGroup`, not a text field (the DEF-192 "Test-2" failure).
- **Fix**: New mouse-independent AX paste for Conductor in `app_fast_paste`, behind `HEYVOX_AX_CONDUCTOR`: fire the profile focus_shortcut (Cmd+L) → read the **live keyboard-focused** element (not the mouse-captured leaf) via `AXUIElementCreateApplication(app_pid).AXFocusedUIElement`, walk to its AXTextArea → set AXValue with read-back verify + restore-on-fail → submit with a **CGEvent** Return (in-process). Falls back to the proven osascript Cmd+V on any failure (clipboard is pre-set; the verify prevents double-insert). The first naive integration was actually *slower* (1268ms, second osascript for Enter). Fixed in three measured steps: (1) Enter via in-process CGEvent → 638ms; (2) skip `set frontmost` when the target is already frontmost (per `project_set_frontmost_focus_disruption`) → 543ms; (3) fire the focus shortcut (Cmd+L) via in-process CGEvent too — the osascript *subprocess spawn*, not set-frontmost, was the real cost under load → **130ms** (focus=84 / axset=22 / enter=1 ms). Net: ~8x faster than the 1036ms osascript path, mouse-independent, correct submit confirmed live (no stray shortcut char). The CGEvent Cmd+<char> posts an explicit Command key press/release around the char (flag-only combos are the finicky part of synthetic modifier events) and maps the char via a US/German-QWERTY keycode table.
- **Found by**: Franz's "paste muss viel schneller" + a per-step latency timeline; then a standalone in-situ probe (his idea to re-test "because many Conductor updates") proved AXValue-set now submits on Enter.
- **Would have caught earlier**: (1) The stale skip carried a hard-coded behavioural claim about an external app's internals with **no periodic re-test** — a fast-path disabled because "framework X breaks" should re-validate on a cadence, not stay dead forever. (2) I shipped the first integration **without sub-timing instrumentation** and it regressed latency; per-step timing should have been there from the first line, not bolted on after a wasted live test. (3) The set-frontmost removal underdelivered (557→484ms, ~73ms) because the osascript *subprocess spawn* dominates under load, not the AppleScript content — under system contention **any** subprocess is the cost; the durable fix was moving the keystroke in-process (CGEvent) entirely. **Gated by `config.injection.ax_conductor`** (default False in code; env `HEYVOX_AX_CONDUCTOR` OR-overrides for quick testing) — anchored in config because the launchctl env var didn't survive a reboot. Franz's `~/.config/heyvox/config.yaml` sets it True. Default stays OFF for the public release until validated on long text (guarded at >2000 chars against the unverified `AXUIElementSetAttributeValue` EXC_BAD_ACCESS report) and non-US keyboard layouts (the CGEvent keycode map assumes US/German physical positions; unmapped chars already fall back to the layout-aware osascript keystroke). Non-Conductor Electron is a non-issue: the path is gated on `conductor_workspace_id`, which only Conductor sets — VS Code/Slack never reach it.

## DEF-193 — Kokoro daemon maps ~230MB of unused libtorch (thinc eager-imports torch it never uses on the MLX path)

- **Date**: 2026-07-08
- **Category**: dead-code / platform
- **Severity**: S3 (no functional fault; idle RSS ~230MB higher than necessary, worsening memory pressure on a machine already running MLX Whisper + parallel Conductor sessions)
- **Symptom**: kokoro-daemon idle footprint ~740MB where `config.py` comments expected ~400MB. `vmmap`/`lsof` showed the full PyTorch native stack (`libtorch.dylib`, `libtorch_cpu.dylib`, `libc10.dylib`, `libomp.dylib`) mapped into the daemon — even though Kokoro TTS inference runs entirely on Metal/MLX and the g2p is the numpy-backed `en_core_web_sm`.
- **Root cause**: The daemon shares the main heyvox venv, which holds torch (hard dep of mlx-whisper, silero-vad, torchaudio, ...). Kokoro's g2p does `from misaki import en` → `import spacy` → `thinc/compat.py:35`, which does an **eager `import torch`** purely to detect an optional torch backend it never uses here. Confirmed by a call-stack tattle: `import spacy → spacy/compat.py → thinc/__init__ → thinc/config → thinc/types → thinc/compat.py:35 → import torch`. torch is imported by **no** heyvox source (0 `import torch` in the tree) and mapped into **no other** running process (the main `heyvox.main` process maps zero torch libs). Pure dead weight, present only because torch is installed in the same venv. A prior naive `sys.modules['torch']=None` block broke spaCy because (a) it wasn't an ImportError thinc could catch and (b) `spacy.load()` eager-loads the torch-dependent `spacy-curated-transformers` entry-points, which then failed hard.
- **Fix**: New `heyvox/herald/torch_suppressor.py` (stdlib-only) installs a `sys.meta_path` finder that raises `ModuleNotFoundError` for `torch`/`torchaudio`/`torchvision` — thinc catches it via its own `try/except ImportError` and sets `has_torch=False`, so spaCy + `en_core_web_sm` keep working. It also patches `importlib.metadata.entry_points` to hide the `spacy-curated-transformers` entry-points so `spacy.load()` skips that unused torch-dependent plugin cleanly (whether or not it's still installed). Called best-effort at the top of `kokoro-daemon.py` before any TTS import; opt out with `KOKORO_ALLOW_TORCH=1`. Also uninstalled the three orphan `curated-*` packages (nothing else depends on them). Verified: `misaki.en` g2p produces correct IPA torch-free; the g2p stack drops from ~250MB to ~94MB RSS at import (~160MB saved), and torch's 234MB `torch/lib` never maps in.
- **Found by**: Franz flagged the daemon's high RAM (`top` ~5.6GB transient, `vmmap` footprint-peak 7.3GB, idle ~740MB) and asked whether a leaner daemon interpreter would help. `lsof` + a call-stack import tattle traced torch to thinc; a separate venv proved unnecessary once torch was shown to be unused dead weight.
- **Would have caught earlier**: No audit compared *loaded* native libs against *used* ones. The transient 7.3GB peak (MLX/Metal work buffers, separately addressed by `mx.clear_cache()`) masked the steady ~230MB torch baseline. Added `test_def193_*` guarding the suppressor mechanics (finder blocks torch, entry-point filter hides curated, `KOKORO_ALLOW_TORCH` opt-out) so a refactor can't silently re-inflate the daemon. General pattern (**P-loaded-not-used**): a process on a kitchen-sink ML venv can silently map hundreds of MB of a heavy transitive dep pulled in only for capability-probing; periodically diff `lsof`-mapped dylibs against what the code actually calls.

## DEF-194 — TTS spoke raw markdown ("asterisk"/"backtick"/"backslash") from prose spliced between literal `<tts>` mentions

- **Date**: 2026-07-08
- **Category**: string-handling / ux
- **Severity**: S2 (spoken output was garbled nonsense — fragments of meta-prose with markdown voiced literally — whenever a response discussed the TTS mechanism)
- **Symptom**: Franz: TTS "gibt Asterisk, Backslash aus, macht keinen Sinn." Herald spoke fragments like "backtick sauber Zeile 466 478 backtick..." instead of the intended English `<tts>` block.
- **Root cause**: `HeraldWorker._extract_tts_blocks` matched `<tts>(.*?)</tts>` anywhere in the response and — worse — overrode a correct line-anchored match with *every* non-anchored match whenever there were more of them (`if len(all_matches) > len(matches)`). When a response explains the TTS mechanism it writes `<tts>`/`</tts>` **literally in backticks** (e.g. `` `^<tts>(.*?)</tts>` ``); the matcher then spliced the prose *between* two such literals — full of markdown (`` ` `` `*` `#` `\`) — into the spoken text, and `texts[-1]` picked that fragment. Kokoro/misaki voiced the raw symbols aloud. Confirmed empirically: 3 of 338 real responses this session extracted a prose fragment instead of the real block — exactly the ones mentioning `<tts>` literally. The watcher path (`tts_helpers.extract_last_tts_block`) shared the same unanchored matcher.
- **Fix**: Strip markdown code (fenced ```blocks``` + inline `` `spans` ``) BEFORE matching `<tts>` in both extractors (`strip_code_spans` in `tts_helpers.py`) — real blocks are never wrapped in code formatting, so only genuine blocks survive; the plain matcher still handles legitimate multiple inline blocks (existing test stays green). Added `strip_speech_markup()` (drops `*` `` ` `` `#` `\`) applied to the final speech as defense-in-depth. Verified: 0 of 339 responses now extract markdown (was 3); 117 extraction tests pass.
- **Found by**: Franz reported garbled speech with spoken symbols and asked me to reproduce it myself (generate + STT round-trip) rather than ask him. A transcript scan of this session's own responses pinpointed the 3 offending extractions.
- **Would have caught earlier**: No test covered a response that mentions `<tts>` literally alongside a real block — the exact shape produced whenever the assistant discusses its own TTS. Added `test_def194_*`. General pattern (**P-self-referential-markup**): any extractor keyed on a delimiter token mis-fires when the surrounding text legitimately *mentions* that token; strip code/quoting context before delimiter-matching, and always test the "content discusses its own format" case.

## DEF-207 — Cue audio crackled on internal speakers (and Bluetooth): 22050Hz cue files fed raw into a mismatched-rate device with no resampling

- **Date**: 2026-07-13
- **Category**: platform / ux
- **Severity**: S3 (cue sound quality only — wake word detection, suppression window, and recording all worked correctly; purely a perceived audio-quality defect, not a functional break)
- **Symptom**: Franz: "listening" cue (confirmed also "sending") crackles audibly and consistently ("immer sehr seltsam") when the default output is the MacBook's internal speakers.
- **Root cause**: All cue files (`listening`/`ok`/`sending`/`paused`/`error`.aiff) are 22050 Hz mono 16-bit (verified via `afinfo`). `heyvox/audio/cues.py::_play_via_sounddevice()` read each file at its own native rate via `soundfile.read()` and passed that raw rate straight to `sounddevice.play(data, samplerate, device=...)` — no resampling. "MacBook Pro Speakers" reports a native `default_samplerate` of 48000 Hz (`sounddevice.query_devices()`); `sd.check_output_settings()` still reports the mismatched 22050 Hz request as ACCEPTED, meaning PortAudio/CoreAudio silently does on-the-fly sample-rate conversion — on a freshly-opened, ~0.5s, one-shot, cold stream with no warm-up, a known trigger shape for click/crackle artifacts. `heyvox/audio/keepalive.py::_load_cue()` (the USB-only warm-stream path, DEF-148/150) already pre-resamples cues via `afconvert` to the target stream's rate before ever touching PortAudio — this exact class of problem had already been solved for USB but was never extended to the sounddevice fallback that built-in-speaker/Bluetooth output actually uses.
- **Fix**: `_play_via_sounddevice()` now resolves the output device's native rate (`_resolve_target_samplerate()`) and resamples the cached cue buffer to it via `afconvert` (`_resample_cue()`, mirroring keepalive.py's existing approach) before calling `sounddevice.play()`. Cache key changed from `cue_file` to `(cue_file, target_samplerate)` since the same cue now needs a different resampled buffer depending on which device is currently default. Falls back to the file's raw native rate (the pre-fix behavior) if the device/rate can't be resolved or afconvert fails — never a regression from the prior state. 2 new tests (`test_def207_resamples_cue_to_device_rate`, `test_def207_cache_keyed_by_target_rate`); 2 pre-existing tests updated for a mocking collision (`subprocess.run` internally uses `Popen`, which those tests already patched — the new afconvert call needed its own explicit `subprocess.run` mock). Verified live against the real production cue file: `audio_cue("listening", ...)` now resamples 22050→44100 Hz to match the currently-active default output device.
- **Found by**: Franz's report of consistent crackling specifically over internal speakers; diagnosed by comparing the cue files' native rate (`afinfo`) against the output device's native rate (`sounddevice.query_devices()`), with the keepalive.py USB fix as corroborating precedent — no audio was actually captured/listened to during diagnosis.
- **Would have caught earlier**: DEF-148/150 already discovered and fixed this exact "cue's native rate vs. device's native rate" defect class, but scoped the fix narrowly behind `default_output_is_usb()` rather than asking whether every output path that skips resampling has the same gap. The sounddevice fallback added later for WW_LATENCY reused the raw-rate-read pattern DEF-148/150 had already moved away from for USB. General pattern: when a fix adds a device-class-specific gate (`if usb: <resample correctly> else: <old raw-rate behavior>`), audit whether the "else" branch carries the same defect the gate was built to fix — see new pattern below.

## DEF-208 — BT-era escalating cooldown demotes a stable USB mic to the built-in for up to 30min on transient blips

- **Date**: 2026-07-14
- **Category**: config / platform
- **Severity**: S2 (user's primary mic silently replaced by the far worse built-in mic for up to 30min at a time; first domino of the 2026-07-09 overnight outage)
- **Symptom**: 2026-07-09 20:56–21:16 the G535 (USB Lightspeed) produced zero-audio blips; each failure escalated the adaptive cooldown (`728s → 601s → 363s → 125s → 8s` remaining, tiers up to 1800s) and demoted to "MacBook Pro Microphone" — repeatedly, for a device that a simple re-open would have healed.
- **Root cause**: The cooldown/fallback machinery (adaptive tiers `[120, 300, 600, 1800]`, demote-to-built-in) was designed for Bluetooth, where "a failed device will fail again" holds. It is transport-blind: a USB dongle transport — stable by construction, failure = transient blip or hardware mute — got the same 30min-demotion policy. Franz moved to USB precisely to escape BT flakiness; the BT-era machinery then became the instability.
- **Fix**: (1) Transport classification in `mic.py` via the CoreAudio transport type (`get_device_transport`/`is_usb_transport`, 30s cache; live-verified: G535 = `'usb '`). (2) `_get_adaptive_cooldown` picks USB tiers `[15, 30, 60]` (cap 60s) for USB transports; BT/unknown keep the original tiers; cooldown log lines now tag the tier table. (3) `DeviceManager._usb_same_device_retry`: in `reinit()` and `handle_io_error()`, a USB device that is still alive in CoreAudio is re-probed and re-opened FIRST (located by name, index-drift-safe) — no cooldown, no demotion, no device-change cue when it heals; only a still-silent device falls through to the old path.
- **Found by**: Log forensics of the overnight outage (`.planning/quick/260710-mic-selection-usb-rebuild/HANDOVER.md`); core insight from Franz — the robustness machinery is BT heritage and actively harmful on USB.
- **Would have caught earlier**: Nothing exercised the cooldown policy per transport class; the mechanism inventory (KEEP/RECONSIDER/REMOVE per transport) only happened in the post-mortem. Tests now pin USB-vs-BT tier tables and the same-device retry (`tests/test_transport_policy.py`). See pattern **P-transport-blind-policy**.

## DEF-209 — -9986 paInternalError storm: corrupted PortAudio context is unrecoverable in-process, daemon looped on it with no escalation

- **Date**: 2026-07-14
- **Category**: error-handling / platform
- **Severity**: S1 (mic dead for every device; manual mic switch impossible; only a manual process restart recovered)
- **Symptom**: 2026-07-10 07:59–08:00 every `Pa_OpenStream` failed with `[Errno -9986] Internal PortAudio error` for EVERY device — including after the keepalive's "PA context dropped for fresh retry" (in-process terminate + fresh `PyAudio()`). The manual HUD mic switch could not bind any device. A manual restart at 08:01 fixed the G535 instantly.
- **Root cause**: -9986 can mean process-wide PortAudio/CoreAudio-connection corruption. In-process context recreation is PROVEN insufficient (survived it all night). No code path recognized the cross-device failure pattern; every recovery loop (keepalive ticks, reinit retries) just spun inside the broken process.
- **Fix**: Storm detector in `mic.py` (`record_pa_open_failure`/`record_pa_open_success` wired into `probe_device_level` and `open_mic_stream`; `pa_storm_detected` = ≥6 -9986 failures across ≥2 distinct devices within 120s, any successful open resets — one flaky device stays a cooldown problem, never a restart). `main.py` checks every 5s EARLY in the loop (the IOError/reinit failure paths `continue` before the idle watchdog block, so the existing DEF-163-style placement would never fire during a storm) and self-restarts via the established execv path. Loop guard: a restart marker (`PA_STORM_RESTART_MARKER`); if the storm re-appears within 600s of a storm restart, no further restart — an error banner ("replug the headset or restart the Mac") is surfaced instead, and the tally is cleared.
- **Found by**: `/tmp/heyvox.log` forensics in the HANDOVER (key signature: `[keepalive] could not open silent stream (attempt 1…12, PA context dropped for fresh retry)`).
- **Would have caught earlier**: DEF-153/163 had already hardened the OUTPUT side of exactly this failure (keepalive owns its PA context + 24-failure escalation) — the input side got no equivalent give-up-and-escalate path. When a recovery mechanism gains an escalation ladder, every sibling loop over the same resource needs one too.

## DEF-210 — Main loop wedged 10.75h with a stale heartbeat: every watchdog lives inside the loop it is supposed to watch

- **Date**: 2026-07-14
- **Category**: error-handling
- **Severity**: S1 (voice control dead all night; no self-heal; user had to notice and restart manually)
- **Symptom**: `Previous instance died without clean shutdown (heartbeat stale by 38702s)` — the daemon stopped iterating its main loop at ~21:16 (2026-07-09) and sat wedged until the manual restart at 08:01. The keepalive thread kept logging all night, so the process was alive and the GIL available — only the main loop was stuck (most plausibly in a PortAudio call that never returned after the -9986 corruption).
- **Root cause**: Every self-heal mechanism — DEF-104 hotplug restart, DEF-163 keepalive-stale restart, the memory watchdog, busy watchdog — runs INSIDE the main loop. A wedged main loop disables all of them simultaneously. The SIGKILL-proof heartbeat file existed but nothing consumed it at runtime; it was only read as a post-mortem diagnostic at next startup.
- **Fix**: `_start_wedge_supervisor` in `main.py` — a separate daemon thread checks the in-process heartbeat (updated every loop iteration) every 30s; if it is older than 300s, it force-restarts via execv (fallback `os._exit(1)`, which launchd's `KeepAlive/SuccessfulExit=false` relaunches). Threshold is generous: legitimate blocking (STT cold-load, paste hangs) tops out around 60–90s. Documented limitation: a wedge that holds the GIL starves all Python threads including the supervisor — the observed incident did not (keepalive thread kept running), so the supervisor covers the failure mode actually seen.
- **Found by**: HANDOVER forensics; the 10.75h heartbeat gap quantified the cost of having no out-of-loop supervision.
- **Would have caught earlier**: The DEF-163 review asked "what if the keepalive can't reopen?" but not "what if the loop that CHECKS the keepalive stops running?". See pattern **P-in-loop-watchdog**.

## DEF-211 — Orphaned guard tests: DEF-104 manual-restart tests merged without their implementation; CI red on main since 2026-07-13

- **Date**: 2026-07-14
- **Category**: regression / integration
- **Severity**: S2 (CI on main red = release-blocking; the guarded feature itself was missing: a manual HUD pick of a USB mic invisible to PortAudio's cache ran the pointless BT-HFP probe instead of the DEF-104 restart)
- **Symptom**: `test_def104_manual_usb_cache_miss_bypasses_bt_hfp_probe` and `test_def104_main_loop_honors_manual_hotplug_restart_request` fail on HEAD and on origin/main; main CI red since the 2026-07-13 pushes. The expected API (`pop_hotplug_restart_request`, `_restart_for_hotplug_candidate`, `_is_coreaudio_live_portaudio_miss`) existed nowhere in the source tree.
- **Root cause**: The guard tests were committed and merged while the implementation they guard never made it into any commit — most plausibly a parallel-session/explicit-path-staging split (tests staged, source files not). The merge landed despite the red check.
- **Fix**: Implementation rebuilt to the tests' spec: `DeviceManager._request_hotplug_restart`/`pop_hotplug_restart_request` + `_is_coreaudio_live_portaudio_miss` (live-in-CoreAudio ∧ missing-from-PA ∧ not-Bluetooth per DEF-147); manual-switch block in `scan()` routes cache misses to the restart request BEFORE the BT-HFP probe branch; `main.py` extracts the guarded restart (BT exclusion + marker loop-guard + execv) into `_restart_for_hotplug_candidate`, shared by the periodic check and the new post-`scan()` consumption of the manual request.
- **Found by**: Full local test run during the DEF-208/209/210 rebuild; cross-checked against origin/main and the CI run logs.
- **Would have caught earlier**: Treating red CI on main as a merge blocker (branch protection / required checks), and never staging guard tests separately from the code they guard. Action item: before merging, confirm the PR's test additions pass against the PR's own tree, and keep main's CI green as a standing release gate. See pattern **P-orphaned-guard-test**.
- **Second sighting — CORRECTED after forensics**: the daemon that ran until 2026-07-14 10:33 logged `WATCHDOG: mic_storm_stale set (DEF-203 PA-corruption storm) — auto-restarting` (2026-07-13 09:52). That code was NOT uncommitted: the whole HANDOVER rebuild was **already built once on 2026-07-10** and committed to `heyvox/launch-p1-install` as **DEF-202** (`74309c73c` transport-aware cooldown + USB same-device retry + demotion banner), **DEF-203** (`2fd390d50` PA-open-storm detection), **DEF-204** (`58b0fe2a8` shared self-restart helper + wedge-watchdog thread) — one hour AFTER the HANDOVER froze its status as "SCAFFOLDED (not built)". A Conductor branch switch stranded those commits (this workspace now checks out `release-1.1.1`), the memory/HANDOVER kept saying "not built", and this session re-implemented the same three mechanisms as DEF-208/209/210 without knowing. **DEF-202/203/204 ≙ DEF-208/209/210** (independent implementations of the same defects; the committed 208–210 line is the live one). Ported from the lost build into this branch: the `_maybe_surface_demotion` banner (the HANDOVER's "never silently demote" goal, which the re-implementation had missed). Deliberately NOT ported: its 60s wedge threshold (real 60s paste hangs, DEF-089, would false-fire; 300s kept) and the flat-15s USB cooldown (gentle 15/30/60 tiers kept). Its `_self_restart` helper dedup remains a nice-to-have for a cleanup pass.

## DEF-212 — Four stale guard tests: DEF-191/192/207 changed behavior intentionally but only updated their own new tests, leaving older guards pinning the outdated behavior (CI red)

- **Date**: 2026-07-14
- **Category**: regression / integration
- **Severity**: S3 (test-only; the production code was correct in all four cases — but the red CI it caused is release-blocking and masks real regressions)
- **Symptom**: On main CI (red since 2026-07-13) and locally: `test_garbled_catastrophic_stt_ratio` (expects the pre-DEF-191 "wall-clock alone discards clean text" behavior), `test_ax_fastpath_for_native_textfield` + 2 `test_target_lock` AX tests (static AX mocks that can't satisfy the DEF-192 read-back verify), and `test_aborts_on_verify_mismatch` (uses `subprocess.run` call-count as a "no Cmd-V" proxy; the abort path's error cue now shells out via DEF-207's afconvert resample). A fifth (`test_aborts_on_clipboard_write_failure`) only passed by accident: the neighbouring test primed the cue cache, hiding the same defect — fixing the neighbour exposed it (cross-test cache coupling).
- **Root cause**: Each behavior change (DEF-191 ratio-guard narrowing, DEF-192 AX read-back verify, DEF-207 cue resampling) added NEW guard tests but did not sweep EXISTING tests that pinned the old behavior or mocked the old call shape. The DEF-191 commit even recorded "149 passed" — a subset run, not the full suite.
- **Fix**: Updated the four tests to the current intended semantics: ratio guard discards only degenerate-shape text (clean+diverse survives, 'doc doc doc' still dies); stateful AX mocks that return what was written (satisfying the read-back verify like a real field); error-cue calls mocked so `subprocess.run` counts stay a pure "no osascript paste" signal, independent of cue-cache state.
- **Found by**: Full-suite run during the DEF-208..211 rebuild; cross-checked against the main CI logs (same failures there — the standing "local env coupling, CI green" assumption was outdated).
- **Would have caught earlier**: Running the FULL suite (not a subset) before committing a behavior change, and treating any red main-CI as a stop-the-line signal. Same family as **P-orphaned-guard-test**.

## DEF-213 — OPEN: startup audio-init wedged 17min holding the GIL — before the heartbeat, the wedge supervisor, and every watchdog exist

- **Date**: 2026-07-14 (observed live; NOT fixed — logged for the next session)
- **Category**: platform / error-handling
- **Severity**: S2 (voice layer dead for 17min after a routine restart with zero self-heal; would look like "it just doesn't start" to a first-run user)
- **Symptom**: After a `launchctl kickstart -k` at 10:33:16, the fresh process logged `Using input: [1] MacBook Pro Microphone` at 10:33:25 — then NOTHING until 10:50:07/08 when everything resumed at once (`Headset detected`, PTT-tap warning `silent for 1005s (received 0 events since start)`, MLX load starting only then, `Ready!`). The stall sits between `open_mic_stream()`/`detect_headset()` inside `DeviceManager.init()`. `launchctl kickstart` itself hung alongside (killed by a 2min caller timeout).
- **Root cause (partial)**: A PortAudio/CoreAudio call inside audio init blocked ~1003s. Crucially, EVERY Python thread stood still for those 17min (the start-preload STT thread only began its 12.9s load at ~10:50:04; the PTT watchdog only spoke at 10:50:07) — i.e. the blocked C call most plausibly HELD THE GIL. Release trigger after ~16.7min unknown (suspiciously close to a ~1000s internal timeout). What provoked the slow init is unconfirmed; heavy parallel PyAudio use by the test suite minutes earlier may have left coreaudiod state behind (cf. the standing "don't bench against the live daemon" rule).
- **Fix**: NONE yet. The DEF-210 wedge supervisor does NOT cover this window (it arms after init, and as a Python thread it starves under a GIL-holding wedge anyway — both limitations were documented on DEF-210 the same day; this incident is their live proof). A credible fix is an out-of-process watchdog: a tiny child process (own interpreter, no shared GIL) spawned first thing in `_setup`, watching "parent alive but heartbeat file never written within N minutes" → SIGKILL the parent so launchd relaunches it. Needs care: must NOT fire while the first-run macOS microphone-permission dialog legitimately blocks init.
- **Found by**: Live log forensics immediately after restarting the daemon to activate DEF-208..212.
- **Would have caught earlier**: Nothing watches the init phase at all; every liveness mechanism assumes the main loop was reached. When adding a supervisor, enumerate the phases BEFORE its arming point — the unguarded window is exactly where this bit.

## Patterns & Process Gaps

- **P-transport-blind-policy** (DEF-208, siblings DEF-101/DEF-146/DEF-147): device-robustness policies (cooldowns, fallbacks, retries, gain) encode assumptions about a transport's failure model; applying BT-era policy to USB made a stable device unstable. **Action item**: any policy keyed on "device failed" must ask "which transport, and what does failure mean THERE?" — classify via the CoreAudio transport type (now available as `mic.get_device_transport`) before choosing the response.
- **P-in-loop-watchdog** (DEF-210, siblings DEF-104/DEF-163): a watchdog that runs inside the control flow it supervises dies with its patient. **Action item**: for every watchdog, name the thread/loop it runs ON and verify it survives the failure it detects; liveness supervision must run in a separate thread (or process, for GIL-holding wedges).
- **P-orphaned-guard-test** (DEF-211): guard tests and the implementation they pin can drift apart when commits stage explicit paths across parallel sessions — the test then documents a feature that doesn't exist while CI burns red. **Action item**: stage guard tests in the same commit as their implementation, run the committed tree's tests before merge, and treat red main-CI as release-blocking.
- **P-unresampled-fallback-path** (DEF-207, sibling to DEF-148/150): fixing a playback-quality defect for one device class (USB, via an explicit resample-to-target-rate step) does not imply other device classes' fallback paths are fine — they may share the exact same "feed the source's native rate straight into the device" gap the fix was written to close, just less visible because no one had reported it yet. **Action item**: when a fix adds correct resampling/rate-matching behind a device-class gate, grep for every OTHER path that reaches the same sink (here: `sounddevice.play()` / `pa.open()`) and confirm each one is equally correct, rather than assuming the reported device class was the only one affected.


- **P-silent-wrong-device** (DEF-172, sibling to DEF-104/148/150/153): a long-lived PortAudio (pyaudio/sounddevice) stream or context can remain "open" and functionally healthy while bound to a device object that CoreAudio has since superseded, if the underlying device swap doesn't cross a boundary the code actually watches (e.g. transport type staying USB→USB across a reconnect). Unlike DEF-104/153's failure mode (loud errors: `-9986`/`-10851`), this variant throws nothing — recovery logic gated on exceptions (`stale` counters, drop-and-recreate-on-failure) never fires. **Action item**: any code opening a PortAudio-family stream against "the default device" should (a) resolve and pass an explicit device index/UID rather than relying on the library's own default resolution, matching it against a live CoreAudio query, and (b) re-validate that binding periodically, not just at open time — "open succeeded" and "bound to the right device" are different claims and only the fix for DEF-172 checks both.
- **P-log-path-split** (DEF-126, recurred as DEF-166): any module that logs independently of the central `main.py` `log()` risks silently diverging from "the log everyone tails." DEF-126 was a wrong-*path* variant (module read a different config field/default than main.py). DEF-166 is a wrong-*transport* variant (module wrote via stderr/fd instead of reopening by path), which is more dangerous because it works fine until the first rotation, then goes silent for the rest of the process's life with no error. **Action item**: any new module-level `_log()` must reopen the resolved log path per call (copy `_resolve_log_path()` from `injection.py`/`target.py`), never `print(file=sys.stderr)` — even though launchd also redirects stderr to the same path at startup, that redirect does not survive `main.py`'s rename-based rotation. `mic.py`, `ptt.py`, `toast.py`, `adapters/conductor.py` still need this fix; audit before the next release.
- **P-unbounded-native-call**: `subprocess.run(..., timeout=...)` is used consistently in this codebase, but PyObjC/AppKit calls (NSPasteboard, NSWorkspace, and likely others) were never given the same scrutiny because they're normally sub-millisecond — until the underlying system service (pasteboard server, WindowServer) itself stalls, at which point they block forever with no recourse. **Action item**: any new PyObjC call added to a single-threaded event-loop path (main.py, recording.py, injection.py, target.py) should go through `_call_with_timeout()` (`heyvox/input/injection.py`) rather than being called directly.
- **P-retroactive-labeling** (DEF-167): any training/label-correction mechanism which infers a label from OUTCOME OR TIMING rather than re-examining the underlying evidence (audio transcript, in this case) risks manufacturing exactly the class of error it claims to fix. **Action item**: any future auto-labeling code path in this training pipeline must be evidence-based (derive the label from re-inspecting the actual data, e.g. via STT) or must run through the same mandatory `tools/quality_gate.py` chokepoint before its output is trusted.
- **P-network-listener-bypasses-tmpdir** (DEF-176, sibling to the Chrome WebSocket bridge `:9285` cut in the Phase-1 install work): the codebase's IPC security model rests on the per-user `$TMPDIR` 0700 directory plus 0600 socket files — a strong mitigation that applies only to filesystem-backed channels (Unix sockets, flag files). Any TCP/WebSocket listener silently opts out of that model: a port cannot be `chmod`'d and is reachable by any co-resident process or (for TCP) any local user account. Both network listeners found in the audit (`hush_host.py` TCP `:9847`, `chrome/bridge.py` WebSocket `:9285`) were alternate transports that had been superseded by the Unix-socket design but never removed, and both lacked auth/Origin protection. **Action item**: prefer `AF_UNIX` for all local IPC. If a TCP/WebSocket listener is genuinely required, it must (a) bind `127.0.0.1` only, (b) authenticate (shared-secret token from a 0600 file, or Origin/Host validation for HTTP), and (c) have at least one real consumer — otherwise delete it. Add a listener-surface guard test whenever such a channel is introduced (see `tests/test_hush_host_security.py`).
